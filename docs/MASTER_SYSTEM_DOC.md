# AI Bot V2 master system document

**Current documentation cut:** 2026-07-16 reconstruction plus coordinated source regression and post-reload runtime reconciliation through 2026-07-18 UTC

**Mode observed:** paper/shadow; live transport disarmed but present in source

**Decision:** NO-GO for live trading and NO-GO for using current paper results as clean promotion evidence.

> Dated **[Post-cut reconciliation (2026-07-16, evening)](#post-cut-reconciliation-2026-07-16-evening)** and **[Adaptive-control repair reconciliation (2026-07-17)](#adaptive-control-repair-reconciliation-2026-07-17)** sections record later evidence. The 2026-07-18 resource cut additionally bounds the resident microstructure monitor's stdout, installs bounded compressed rotation for four high-rate service families, reduces preserved monitor history from about 14 GB to 876 MiB, narrows IDE watcher scope and repairs quoted `PYTHONPATH` in four trainer units. The supervisor poll-amplification source is repaired but was not restarted/deployed; its preserved 10.9 GB event log was still growing at about 112 KB/10 seconds (~0.9 GiB/day). Redis remained about 25.49 GiB under `allkeys-lru` with AOF disabled, including two unexpired evidence objects of about 6.23 GB and 536.8 MB. Corrected archive/hot-cache/consumer source checks pass 87, but no runtime migration ran. Seven research/trainer/replay services are held fail closed and both automatic trainer timers are stopped. Held-service burn-in, trainer authority and trading-performance blockers remain. The NO-GO decision is unchanged.

This is the repository-level entrypoint. The complete current audit is [AI_BOT_V2_FULL_REBUILD_MASTER_AUDIT_REPORT.md](system_audit_2026_master/AI_BOT_V2_FULL_REBUILD_MASTER_AUDIT_REPORT.md); the document map is [REVERSE_ENGINEERING_INDEX.md](system_audit_2026_master/REVERSE_ENGINEERING_INDEX.md).

Older June/July documents remain historical snapshots. They are not current when they claim one trainer, read-only V2 APIs, operational SQL persistence, risk as final paper authority, no order transport, or the earlier service/module/test counts.

### Paper final-fill remediation reconciliation (2026-07-21 source only)

An isolated, undeployed paper-only repair now separates provisional candidate
admission from economic fill materialization. Cumulative margin/economic
reservation precedes a fresh final PIT and revocable-control reread; the paper
fill write invariant is rechecked, then a hash-bound materialization receipt
stamps `entry_time`/`execution_time` immediately before the sole economic-list
append. Missing final authority, a same-key control flip, a pre-seal stamp,
receipt mutation, or persistence mismatch fails closed. The preemptive runtime
clock is captured once after canonical alt-data lookup and is sealed in its
input material; canonical optional-altdata absence/error carries a consumer
`altdata_lookup_observed_at` without inventing producer clocks. Missing thesis
timeframe is row-local and no longer terminates the cycle. Exact-zero loss
semantics are isolated to an authenticated paper adapter; shared/live evaluator
defaults are unchanged. This is source/test evidence only: no service was
restarted and no Redis or exchange state was changed. Static policy debt is
classified in [PAPER_RUNTIME_STATIC_THRESHOLD_DEBT_2026-07-21.md](system_audit_2026_master/PAPER_RUNTIME_STATIC_THRESHOLD_DEBT_2026-07-21.md); no threshold semantics were relaxed in this slice.

## System identity

AI Bot V2 is a Redis-centered distributed crypto trading research platform. On the audited workstation it comprises market/provider ingestion, temporal feature assembly, a native GPU policy/trainer, prediction/signal publication, orchestration, risk records, paper execution/lifecycle/accounting, portfolio/guardian/feedback loops, a FastAPI backend, React/Vite web client, SwiftUI/watch/CLI clients and a large autonomous operations/evidence layer.

It is not reproducible from a Git clone alone. Effective systemd units/drop-ins, mutable runtime/model/replay data, Redis state/config, local auth, ignored frontend build, machine packages/CUDA and Cloudflare provider-side routing are also required.

## Adaptive-control repair reconciliation (2026-07-17)

The current worktree contains scoped paper/training/control-plane repairs; live exchange behavior was not changed. The low-level contract and exact blast-radius map is [ADAPTIVE_END_TO_END_CONTROL_AND_ACCOUNTING_2026-07-17.md](system_audit_2026_master/ADAPTIVE_END_TO_END_CONTROL_AND_ACCOUNTING_2026-07-17.md). Safe observation, validation, expected fail-closed states and post-reload monitoring are in [OPERATOR_VALIDATION_AND_MONITORING_RUNBOOK_2026-07-17.md](system_audit_2026_master/OPERATOR_VALIDATION_AND_MONITORING_RUNBOOK_2026-07-17.md).

Source/runtime mitigation recorded at this cut:

- HOLD/flat/close/hedge no longer becomes a synthetic LONG/SHORT proposal; favorable SHORT edge is scored in side-directional space while the signed edge is retained.
- Orchestrator paper signals use `rd_dec_<prediction>` correlation, remain `RISK_PENDING`, and do not grant fill permission.
- Ordinary A+ paper admission requires an exact, fresh, resolved canonical risk record whose normalized action is allow; missing/mismatch/stale/deny defaults closed.
- The A+ regime, higher-timeframe, and trade-tape freshness guard now requires an explicit timezone-aware generation clock, rejects naive/future/conflicting generation aliases, and—when `available_at` is supplied—keeps it distinct, requires `generated <= available_at <= evaluation_time`, and checks both clocks against the configured age. Legacy producer rows with no `available_at` can still qualify on generation time alone, so this is a scoped source hardening rather than complete per-source availability proof.
- Paper admission now snapshots mutable Redis-backed A+/entry evidence before allocation. The entry gate can run with `runtime_evidence_preloaded=true` and `redis_client=None`, so cascade/liquidation, adaptive confidence floors, outcome memory, and side performance cannot be reread at a later generation; a typed hashed evaluation receipt is copied to intent, allocation/model inputs, and risk. Preemptive edge control similarly has no Redis/OS reads, deep-copies one caller tuning snapshot, owns an aware decision time, and binds its complete canonical input hash into the decision ID. The cycle caller and summary use the same tuning state, whose hash is equality-checkable with the entry snapshot. This is source hardening, not deployed or full-materialization proof.
- Bounded per-symbol/timeframe signal discovery now works even when the aggregate signal key is empty; paper-only `open_*`/`proceed_*` aliases normalize only the orchestrator stage and never replace canonical risk ALLOW.
- Generic confidence-only gate relaxations and fast-path admissions are retired from effective control flow; fee, temporal, allocator, churn, freeze and common fill invariants remain binding.
- Temporal trainer windows require immutable decision time and cannot fall back to input order. `TrainingExample` also resolves the latest valid label-availability time from explicit outcome/exit timestamps or a positive label horizon; the in-cycle validation split is chronological, keeps equal decision timestamps together, and purges every training row whose label was not available before validation begins.
- Trusted replay subtracts costs from both directional counterfactuals and separates best-action targets from actual behavior outcomes.
- The audited old-generation PPO path expected `CATEGORICAL_SAMPLE` plus `RAW_LOGITS_SOFTMAX_V1`. A 92-row closed-trade recount found 2 `ppo_on_policy_entry_fields_present=true`, 42 false and 48 absent, with 2 populated `old_log_prob`; all 92 lacked the required sampling-mode/distribution identity. Trainer `_has_on_policy_ppo_fields` admitted **0**, and runtime metrics reported `ppo_on_policy_rows=0`, `ppo_rows_consumed=0` and 2,014 missing-field rejections. The only stored fingerprint was a static paper-owner constant, not a served checkpoint/model-content hash. This remains the authoritative deployed/runtime observation.
- The current, undeployed source repair replaces that historical behavior contract with `v2_positive_edge_on_policy_behavior_receipt_v1`, `CATEGORICAL_SAMPLE`, `POSITIVE_EDGE_MASKED_RAW_LOGITS_SOFTMAX_V1`, source `NATIVE_CUDA_POLICY_CATEGORICAL_SAMPLE` and mask `PIT_AFTER_COST_POSITIVE_ENTRY_ACTION_MASK_V1`. Its `v2_adaptive_on_policy_paper_lane_plan_v1` admits only final, `TRAINABLE` rows satisfying `candle_close <= feature_cutoff <= available_at < decision_time` and independently requires `candle_close < decision_time`. It also requires symbol/timeframe, an exact served-policy fingerprint, a real checkpoint generation, 64-hex weight/evidence digests, fitted directional profitability confidence, valid raw logits, exact adaptive cost provenance and a strictly positive LONG or SHORT edge after that cost. Candidate credit is the bounded geometric mean of normalized policy entropy, profitability uncertainty, positive-edge quality and paper free-margin headroom; `floor(carry + sum(credit))` creates the sample budget, multi-candidate cycles reserve at least one ordinary lane, single-candidate cycles alternate through an ordinary-lane credit, and no market-static sampling threshold is used.
- A selected source candidate keeps HOLD plus only strictly positive after-cost directional entries in its action mask, applies masked softmax to the raw logits and samples with a cryptographic 53-bit uniform draw. The self-hashed receipt now requires and binds checkpoint/weight/evidence identity, exact served parameters, symbol/timeframe, feature tensor/vector, strict clocks/finality, raw/masked probabilities, sampled action/probability/log-probability, policy value, plan hashes and `v2_exact_adaptive_cost_provenance_v1`. That cost envelope embeds and hashes the complete order-book publication, recomputes spread, depth/impact and `2*fee+spread+2*impact`, proves source sequence continuity, and binds adaptive median-plus-MAD expiry, publication TTL and consumer observation with no fallback or conservative floor. Configured paper/shadow cost identity is now explicit: `paper_cost_fee_schedule_evidence_v1` binds `CONFIGURED_TAKER_FEE_BPS_PER_SIDE`, its rate/source and SHA-256, while `paper_cost_notional_configuration_evidence_v1` binds reference notional/value/source and SHA-256; exact admission independently rederives both envelopes. This proves the configured paper/shadow inputs, not the actual exchange account's fee tier, discounts or maker-versus-taker applicability, which remains a live-transfer boundary rather than a paper exact-lane blocker. Exact receipts are now immutable Redis records written with no fixed expiry (`ex=None`), so proof is not deleted merely because a position/training delay exceeds seven days. Carry/ordinary credit remains process-local, and configured publisher coverage/confidence/edge minima remain static downstream gates. The deployed cost key observed on 2026-07-18 was the old schema with null adaptive clocks/provenance, so current exact sampling must reject it until the producer is restarted and acquires four distinct source clocks (three intervals). No source contract here supersedes runtime PPO 0/0, grants A+, or authorizes live routing.
- Redis is no longer intended to be the sole behavior-receipt retention plane. `durable_behavior_receipt_archive.py` writes `v2_durable_behavior_receipt_archive_v1` as an fsynced, create-or-identical, content-addressed JSON blob under `.local_data/v2_native_trainer/durable_behavior_receipt_archive`, then records immutable `v2_behavior_receipt_lifecycle_event_v1` files in order `PUBLISHED → ENTRY_ACCEPTED → OUTCOME_FINALIZED → TRAINER_CONSUMED`. A per-receipt `fcntl.flock` spans lifecycle read/check/write/readback; duplicate stored event types are corrupt/fail closed. Publisher archive+`PUBLISHED` success precedes the no-expiry Redis cache write and exact route eligibility. Paper entry re-verifies the blob and configured fee SHA before `ENTRY_ACCEPTED`; feedback re-verifies that binding before `OUTCOME_FINALIZED`. The final `TRAINER_CONSUMED` event is defined but not yet called after the durable SQLite disposition, so lifecycle status remains `retention_required=true`. There is no archive garbage collector: blobs are presently never deleted, preventing premature evidence loss but creating unbounded local-disk growth. The content hashes are self-authenticating/tamper-evident, not keyed authentication or backup proof.
- The confidence output now has one bounded semantic contract: for a selected LONG or SHORT, it is the action-conditioned probability that recomputed realized net PnL is strictly positive under `realized_gross_pnl_usd - entry_fee_usd - exit_fee_usd - entry_slippage_usd - exit_slippage_usd + funding_pnl_usd`, with gross itself recomputed from action/side, entry price, exit price and closed quantity. V2 admission requires the exact paper accounting versions/formula/rate scope, entry/exit notionals, pro-rata partial-close allocation arithmetic, every fee/slippage component and source, signed funding aliases, complete provenance, no fallback, causal exit-cost availability, and strict aware PIT clocks. `build_close_event` now uses the observed exit half-spread exactly, without the former 0.25-bps minimum; configured slippage is used only when spread evidence is missing, marks fallback, and is therefore excluded from exact PPO truth. Claimed gross/net/sign/outcome values are consistency checks only. Calibration remains `v2_profitability_confidence_calibration_v2`, fit on purged training rows only and bound to the post-fit parameter fingerprint. Source now defines `v2_checkpoint_bound_confidence_promotion_gate_v1`: it rederives global/LONG/SHORT same-row Brier/ECE non-regression and requires paired Brier mean/standard error/one-SE upper bound plus ECE jackknife uncertainty/one-SE upper bound to prove non-regression without a configured market sample threshold. This gate is necessary only and never self-authorizes serving. No held runtime has proved nonzero eligible V2 targets, both-direction fit, untouched-forward reliability, compatible checkpoint promotion, A+ or 1000x performance.
- Paper restart reconstruction now has an exact source contract for partially closed positions. `PAPER_OPEN_POSITION_RECONSTRUCTION_V1` plus canonical SHA-256 binds position/generation/version, aware ordered entry/open/reconstruction clocks, symbol/side, remaining quantity, average entry, ordered source fills, realized PnL and incurred/remaining/allocated entry fee/slippage ledgers with their materialized fallback rates/sources/status. Only a hash-valid round-tripping OPEN_POSITION paper snapshot can seed economics before fill replay; legacy partial, tampered, future-clock, incomplete-ledger or mixed complete/incomplete same-side basis fails closed. A whole generation is suppressed only by explicit final-close plus quantity conservation. Historical netting fills require a versioned hash receipt bound to close/generation/fill/side and `input = consumed + residual`, and accepted-fill disk compaction retains both evidence envelopes. This source passed 509 full paper-management/persistence tests, 16 focused adversarial/persistence tests and four existing compaction/rehydration selectors, but no runtime restart, migration or release occurred.
- A paper-only Ridge champion/challenger now enforces durable snapshot hashes, finality/latest-unclosed proof, aware PIT clocks, no future-label features, explicit fee/slippage/funding costs, action-specific nets, chronological purged train/validation/holdout separation and untouched nested time/symbol clustered-bootstrap holdout evaluation. It cannot fill, promote A+ or route live. Sixteen focused tests passed, but a real 200-snapshot probe completed in 0.19 seconds at 29,208 KB peak RSS and produced **0 evaluable rows**: 145 PIT/finality/hash-admitted rows each lacked explicit fee and slippage evidence, while 55 lacked latest-unclosed exclusion proof. The four-hour embargo therefore left train/validation/untouched-holdout counts at 0/0/0; blockers explicitly included incomplete cost coverage, insufficient distinct decision groups and empty post-purge train/validation partitions. No model, edge claim or paper enablement exists; “+30 bps” remains unproven.
- Checkpoint promotion now hard-requires a PIT-safe purged validation split plus positive held-out serving-policy after-cost mean edge and a positive one-standard-error lower bound. Missing/negative evidence fails closed, and a rejection streak is diagnostic only—it cannot force promotion through the PIT/edge gate.
- The persistent trainer's validation guard is explicitly enabled in the versioned and effective service environment; the former disabled-guard shortcut is removed. The offline wrapper now propagates a child trainer's nonzero exit to systemd instead of printing and continuing, while its versioned unit uses bounded `on-failure` restart policy. Checkpoint retention now emits `native_cuda_trainer_checkpoint_retention_manifest_v2`, scans only `v2_hybrid_ckpt_*` JSON/NPZ artifacts across serving/candidate/rejected stores, excludes its own control manifest, pins active serving, latest candidate, pending-claim reconciliation artifacts and SQLite/WAL/SHM, deletes only complete unpinned pairs and pins all rejected artifacts when claim state cannot be read. It recomputes remaining bytes and can recover the newest complete checkpoint identity when a manual status probe has no caller-supplied ID. Runtime publication preserves outer schema `native_trainer_runtime_status_v1`, separates the manual cycle PID from systemd service liveness/PID, and reports process/CUDA readiness only from observed evidence. These are source and unit-contract repairs, not proof that the held trainers learned or served a checkpoint.
- The adversarial trainer verdict remains **TRAINER NOT READY**, but the former negative probes must now be read as historical reproductions rather than current source behavior. Helpers reject equal close/decision clocks, naive timestamps, `done != true`, non-integer/negative trajectory indices, arbitrary reward/finalized-outcome mismatches, sampled/corrupt/previously consumed rows falling back to outcome supervision and interior-row cache mutations (`v2_trainer_full_ordered_tensor_cache_digest_v1`). The main `run_hybrid_trainer_cycle` now integrates distinct serving/candidate/rejected stores, startup artifact/ledger reconciliation, consumed-row removal, a fixed-point exact-claim planner, optimizer write-ahead fence, candidate/confidence/serving decisions, persisted candidate/rejected/serving artifacts, ledger disposition and verified prior/promoted serving restore. Decision time is microsecond-aware. SQLite `v2_exact_ppo_consumption_ledger_v3` update keys bind receipt + finalized-outcome + parent fingerprints; dead post-fence ambiguity is consumed as `CRASH_AMBIGUOUS_OPTIMIZER_ATTEMPT_CONSUMED_FAIL_CLOSED`. This is tamper-evident, not authenticated. Source integration still is not a controlled trainer run: sole/latest PPO validation-only starvation, the missing post-ledger `TRAINER_CONSUMED` archive hook, process-local lane state, static downstream publisher gates, deployed old cost schema and cold-start→candidate→promotion→restore/crash burn-in remain paper/trainer blockers. Actual exchange-account fee-tier/discount/maker-taker authority remains a separate live-transfer blocker. Keep all trainer holds/timer stops in place.
- Trainer publication propagates exact candle-finality and MASA/PPO cutoff lineage into prediction/orchestrator/risk/signal surfaces without inferring missing finality.
- Successful replay/prediction publication now commits exact persisted metadata back to the lineage payload; failed publication suppresses downstream lineage/risk. Risk alias records embed the alias ID while retaining canonical ID and `alias_of` provenance.
- Trainer lineage is explicitly a non-authoritative proposal and no longer writes canonical risk/orchestrator/index keys. The risk gateway owns canonical risk; canonical per-ID orchestrator persistence and a complete paper-specific risk policy remain blockers.
- A rejected trainer candidate is never served unless prior-checkpoint restoration is verified; otherwise policy backtest, inference, prediction and lineage are suppressed with explicit no-evidence telemetry.
- Executed quantity × price is canonical paper notional. Open-book used margin accepts only executed/explicitly validated current-capital evidence; recommendation-only leverage cannot reduce margin, uncapped >1x falls to 1x, above-envelope leverage blocks, and missing maintenance evidence makes liquidation unavailable and reservation fail closed. Reconciliation is written to `current_capital_accounting` without mutating the original `adaptive_allocation` provenance.
- Paper risk maxima can only remain at or shrink below the base envelope. Leverage can exceed its 3x base only with favorable realized evidence plus positive after-cost lower-bound, provenance, finite liquidity/regime context and point-in-time timestamps. Current source continuously interpolates from base toward the operator-authorized global/symbol ceiling—75x BTC/ETH, 50x SOL/LTC/XRP, 20x other—while adverse or missing evidence contracts toward 1x. The allocator independently clamps each candidate to its authorized symbol tier. The paper loop derives/wires the evidence fields from strict-governed outcomes and current valid signal context, but its receipt is cycle-local and incomplete—not persisted with exact admission/output/rejection/contributor identity. Positive LCB magnitude is not yet used beyond its admission gate, and no controlled reload has proved >base flow.
- Candidate paper leverage now interpolates continuously from 1x to the dynamic envelope using calibrated confidence, after-cost edge relative to costs/volatility, liquidity, regime, drawdown and correlation; a recommendation invariant violation hard-falls to 1x and scarce margin cannot force a higher rung. Paper maintenance must be finite `0 < rate < 1` or allocation/counterfactual liquidation math fails closed. Cross-paper mode is simulated candidate-level telemetry and can be downgraded to isolated; it is not an account-wide cross-margin engine or exchange mutation.
- The operator-approved paper recommendation implements per-symbol ceilings BTC/ETH 75x, SOL/LTC/XRP 50x and other alts 20x plus a five-ATR liquidation-safety rule. Preserve that implementation. Current binding source now imports these ceilings: the dynamic envelope can earn continuously from the 3x base toward the authorized tier only from favorable realized/PIT evidence. Allocator selection is the minimum of the Phase-8 recommendation and its continuous market-evidence target inside the dynamic/symbol cap, so the recommendation's five-ATR liquidation ceiling cannot be bypassed. The authenticated bracket path builds integer choices `1..floor(min(signed max_initial_leverage, authorized symbol ceiling))`. This is not a static leverage grant: binding still takes the conservative minimum of authenticated account-specific bracket leverage, exact post-fill notional, volatility/edge uncertainty, executable liquidity/slippage, correlation/cascade, maintenance/liquidation, drawdown, free margin and concentration. Current negative edge contracts the envelope and earns no high-leverage lift; runtime bracket binding remains blocked.
- The paper cascade diagnostic now joins each open position by symbol to `paper_account_margin_status.position_margin_rows` before portfolio shock math. Missing, duplicate, invalid, nonpositive-notional/rate or leverage-below-1 evidence makes `paper_cascade_margin_join_v1` BLOCKED and suppresses the portfolio-level result as `UNTRUSTED_MARGIN_EVIDENCE`. `cross_margin_liquidation_v2` accepts the audited exchange/paper quantity, mark and leverage aliases and reports missing leverage rather than inventing it. A bounded runtime component receipt at 04:40:17.785Z was PASS with 2/2 positions joined, complete leverage/maintenance evidence, maintenance margin $0.55807547, no directives and no modeled breach. This does **not** enable cross margin or prove a paper cycle: allocations remain `isolated_paper_simulated`, the engine is pure diagnostic math, and its fixed BTC shock/beta table remains adaptivity debt.
- Paper exchange-step quantization now occurs before margin configuration. The allocator rechecks `min_qty` and `min_notional` on the rounded-down quantity without rounding a reduced order back up, then derives notional, margin, liquidation-distance dollars, conservative raw-ratio maintenance and cross-margin stress from that exact final quantity. This restores `N = abs(q*price)` and `M = N/L` at the allocation boundary; the live branch was intentionally unchanged.
- A hardened isolated Binance USD-M signed-read connector can cache leverage brackets under exact environment/trader/credential-reference namespaces and fail-closed select maintenance rate, cumulative deduction, and exchange leverage ceiling for total post-fill symbol notional. Producer and consumer require the same account-specific adapter binding plus a separate >=32-byte HMAC key that is not the exchange secret. Selection checks content checksum, HMAC, exact binding, and `available_at <= decision_time <= consumer_observed_at <= current_checked_at < expires_at`. The paper loop has shared-context, conservative high-water allocation, exchange-ceiling, and whole-net-position reselection plumbing. An independent glue audit then found high-water overshoot, partial lifecycle provenance, dead Tier-0 liquidation-distance, cross-versus-isolated math, early decision-time, alias, and full-run coverage defects. Current source hard-blocks notional above the conservative high-water and a more restrictive observed tier, derives Tier-0 distance from current mark and the canonical liquidation estimate, forces any non-isolated paper recommendation back to `isolated_paper_simulated`, requires lifecycle checksum/HMAC/binding/environment/key/source/time fields with strict structural and temporal checks, captures a per-candidate allocation decision, and normalizes canonical nested, raw selector-shaped, and flat persistence schemas. The lifecycle boundary still accepts a plain upstream-`prevalidated` mapping and does not cryptographically recompute its HMAC; the first per-candidate PIT inventory required a second hardening pass across 47 component clocks; and there is no deployed evidence poller, full `run_once` context→allocation→lifecycle test, accepted post-audit runtime receipt, or advanced-indicator strict-contract proof. The connector is strong, but its paper trust/control integration remains uncertified and is not allocator authority; `initialLeverage` is a ceiling, never a recommendation.
- Hedge-aware entry sizing no longer assumes that a requested hedge will fill. Until an atomic funded/cap-safe hedge proof exists, it sizes against the full unhedged stop, keeps amplification at 1.0, and emits `DISABLED_NO_ATOMIC_FUNDED_HEDGE_PROOF`. A separate paper-runtime interlock now overrides environment/Redis hedge enablement with `HEDGE_DISABLED_NO_ATOMIC_FUNDED_EXACT_LINEAGE_EXECUTION_PROOF`; after the 16:00:27 EDT restart, its 20:00:29.746 UTC status was disabled with zero synthesized fills even though the environment requested enablement. The dormant synthesis/lifecycle implementation remains unsafe and must not be re-enabled.
- The halted-book probe's undefined-confidence crash, same-cycle slot over-admission, gate-erasure behavior, reservation leak, and quarter-size accounting mismatch are mitigated in source. Only explicit finite calibrated confidence is accepted; pre-existing non-performance blockers are preserved; attempts and final slot/symbol capacity use separate token states; downstream rejection releases the pending token; and capacity finalizes only at common accepted-append paths. A second allocator pass multiplies `paper_risk_budget_fraction` by 0.25 and applies it to risk plus gross-notional ceiling before re-deriving every capital/liquidation field. Reduced probes block rather than round up below venue minimum. Exact tagged probe-generation/open accounting, timestamp normalization, adaptive replacement of remaining heuristics, and causal probe-policy outcome stratification remain open.
- Outcome-memory source repair now separates actual close event time, outcome `available_at`, and processing `last_updated`; freshness uses the latest valid outcome availability, not rebuild time. It requires aware PIT/lineage/economic fields, rejects future/impossible rows, never zero-fills missing PnL/return, records rejection counts, sorts by parsed close time, and replaces lifetime loss accumulation with rolling peak-to-trough diagnostics. It remains a P0 governance blocker: a degraded aggregate still becomes generic allowed advisory after the fixed stale interval rather than a typed probe-only request; source-hash checking is structural/cross-field, not cryptographic producer authentication; rejected rows are counted as quarantined but no durable quarantine surface is proven; and probe/full-policy outcomes are not causally stratified. It must not authorize ordinary A+.
- Account-wide paper capacity charges canonical open margin first, preserves the adaptive buffer, and cumulatively reserves all current-cycle candidates before lifecycle; insufficient or incomplete accounting fails closed.
- The former adaptive-sizing operator status embedded every full candidate allocation and grew to about 5.29 GB while the paper process reached about 6.7 GB RSS and 161 GB of writes. Current source leaves canonical allocation/decision/ledger rows unchanged, streams each full publication row through an exact canonical SHA-256, and publishes at most five bounded `paper_adaptive_sizing_operator_projection_v1` rows plus ordered per-row hashes, aggregate hash and `paper_candidate_canonical_aggregate_contract_v1`. The aggregate carries exact all-candidate zero-liquidation, hedge and capital/leverage/margin facts; Guardian validates coverage/hashes, while OOS classifies a projection as `operator_projection_context_only`. The first reload failed at 873,406,311 bytes and 596/621 null hashes; later read-only artifacts were bounded. A controlled `--once` run then exited 0 in 69.85 seconds with **4,925,812 KB peak RSS** (~4.70 GiB), zero swap, 597 intents built, 0 accepted, 597 blocked and 551 Redis keys written; its process/resource classification was `PRODUCTION_OK`, not trading promotion. Its temp artifact was 5,017,701 bytes and canonical artifact 4,160,870 bytes with no `Infinity`/`NaN`. Adaptive sizing had 622 sources, five projections, zero unhashable rows and a valid Guardian contract; current shadow had zero sources and PASS. Historical truth was not rewritten: persistent history retained 132 sources/five projections/132 unhashable legacy-nonfinite rows and `FAIL_CLOSED`. Exploration was ACTIVE with `NO_TRADE=597`, `SHADOW_ONLY=214`, all 597 blocked. The service remains held/inactive; repeated resident RSS/write/restart burn-in and canonical-history quarantine/remediation remain open.
- Host-resource pressure is broader than the paper-status artifact. The reconciliation sample showed Redis at about 25.49 GiB under a 32 GiB `allkeys-lru` cap with AOF disabled. `v2:guardian:pit_prediction_observations` was an unexpired 6,054,941-row list using 6,230,529,272 bytes; `v2:trainer:feedback:counterfactuals` was an unexpired 536,827,021-byte string. Prior service peaks were 6.97 GB plus 6.4 GB swap for adaptive productivity, 22.94 GB Guardian, 16.13 GB persistent trainer and 21.25 GB offline GPU trainer. A self-healer restarted stopped processes, so temporary user-systemd holds now cover seven exact services: adaptive productivity, Guardian, edge replay factory, persistent trainer, offline trainer, scheduled pretrain and native PPO/MASA continuous guard. All were inactive/dead with `RefuseManualStart=yes`; the scheduled-pretrain and continuous-guard timers were stopped, and no live unit was touched. The corrected retention source now has transactional Guardian delivery, bounded legacy migration, explicit-cost counterfactual archive/hot set, authoritative SQLite Guardian consumption, dirty-row quarantine, fsynced sink verification and a complete migration/outbox/cursor trim gate; 87 combined and 18 focused consumer tests pass. It was not deployed against runtime Redis. The 10.9 GB supervisor event log still grew about 112 KB/10 seconds because its source-side suppression was not restarted/deployed. Controlled migration and repeated burn-in remain open; no Redis evidence was trimmed or deleted.
- The adaptive-productivity counterfactual sweep formerly accumulated every feasible row across candidates in `all_results`. It now streams exact feasible counts, axis-value sets and hedge-accounting aggregates; retains at most 20 audit samples plus one best row per candidate; stamps `feasible_rows_materialized_across_candidates=false` / `feasible_rows_aggregated_streaming=true`; and prunes missing/invalid maintenance evidence instead of assuming 0.005. The 323-test allocator/productivity/Phase-8 lane passed. A real probe processed 4,592,832 feasible configurations in 3:08.82 at 1,733,372 KB peak RSS, zero swap and ~23 MB output. Exit 2 was a truthful semantic `NO_GO` because OOS live-grade revalidation was missing, not a crash or performance success. Guardian/trainer/productivity holds remain until controlled burn-in.
- The trainer's study-only leverage scorer no longer ties every grid value back to 1x: it uses PIT-complete lower-bound edge and expected-log-equity curvature. Its existing production caller lacks the new evidence and therefore returns no recommendation; >1x is not yet production-flowing through that study.
- Paper position identity is generation-aware, so a later reopen cannot be suppressed or inherit state merely because a signal/prediction ID or symbol recurs.
- Close and outcome telemetry now preserve the full whole-position maintenance lineage—rate, cumulative deduction, mark notional/time, selected bracket/ceiling, checksum/HMAC, account/environment/key binding, availability/expiry/observation, and evidence status—rather than dropping it after lifecycle.
- G10 now requires mathematical capital consistency and sufficient data. G12 and the rare-event producer require zero warnings.

This is mitigation, not A+ or profitability proof. A+ has **not** been achieved. Read-only evidence at 2026-07-17 17:48–17:55 UTC remained adverse: equity $2,991.1964 from $3,000, realized PnL −$9.9269, performance circuit `HALTED_PERFORMANCE`, 23 post-repair/governed closes, PF about 0.704, weighted expectancy about −7.701 bps, and trajectory binding constraint `NO_A_PLUS_SUPPLY`. Fresh G11 was FAIL (0/5 configurations; mean −5.9123 bps; PF about 0.863; win rate 41.18%). G12 contained 9 PASS and 8 WARNING, which is FAIL under corrected semantics. Forty-five of 86 historical closed rows violated `gross_notional_usd ~= allocated_margin_usd * effective_leverage`; they remain historical failed evidence, not rows to rewrite.

The coordinated source regression was green and the post-fix compact-status lanes now supersede their affected checkpoints: **533/533 paper-loop** (prior 530 and coordinated checkpoint 526), **331 trainer/PIT**, **480 lifecycle/paper-trade-management**, **207 allocator**, **16 adaptive-tuning** (prior checkpoint 15), **72 recorder/integration**, **92 orchestrator/risk**, **99 preemptive/A+**, and **77 portfolio/microstructure** tests. Compact-status dependents passed **91/91 OOS** and now **33/33 Guardian**; the post-fix preemptive-edge-control lane passed 77/77; paper cascade/cross-margin join passed **13/13** focused cases. The final allocator + adaptive-productivity + Phase-8 lane passed **323/323**, superseding the 223-test leverage checkpoint; it includes the regression that keeps high-volatility BTC at 1x despite a 75x envelope and the streaming counterfactual repair. A separate six-case authenticated-bracket ladder selection passed with 531 deselected. Compilation and scoped F821/F811 Ruff were clean. These lanes can overlap and must not be summed as a unique-test count. The premium-index producer repair separately passed 14 focused tests, both affected producer modules' full 10-test set, compilation and relevant Ruff checks. They establish source consistency and bounded probes, not deployed profitability, controlled-service RSS/write-rate stability, Redis/host retention safety, clean historical canonical rows, an accepted/WSS-complete paper cycle, A+, exchange account binding, or the 1000x objective.

The BTCUSDT 1m prediction observed at 18:16:27 UTC predated publisher-lineage deployment and was safely blocked. A fresh row at 18:44:20 UTC then carried confirmed finality, exact candle times, MASA/PPO cutoffs, PPO decision time, and successful replay metadata with valid temporal ordering. It remained blocked for explicit non-directional/confidence/after-cost-edge reasons. This proves the repaired prediction-stage success shape is deployed, but not yet routeable cross-surface lineage or failure-injection behavior.

The system was not stuck at 1x: the same 86 rows contained 43 at 1x, 41 at 2x and two at 3x. Higher leverage cannot repair negative expectancy. The 1000x-in-90-days value is explicitly a research objective, not a guarantee; current evidence does not support it.

Open P0/P1 work includes complete per-source point-in-time feature lineage, preserving the expanded 47-clock allocation contract as every economically active schema evolves, strict advanced-indicator timing or inclusion in that receipt, a full `run_once` allocation→lifecycle proof, real sampled behavior-policy generation and full trajectory semantics for PPO, globally enforced immutable final-holdout isolation, canonical per-ID orchestrator persistence plus a dedicated paper risk policy/authority, a durable cross-process reservation transaction/journal, persisting/versioning the dynamic-envelope admission/output/rejection receipt, separately approved migration of the explicitly stamped live 0.005 maintenance compatibility path and audit of other stress-helper callers, fresh post-repair lifecycle/accounting outcomes, and a verified account-wide margin-call/cascade-liquidation mechanism. Static operating thresholds also remain and must be classified/replaced where they are neither protocol nor immutable safety constraints.

The halted-probe and outcome-memory defects above are also P0. Preserve the hedge runtime interlock until executable pricing, an independent canonical hedge decision, exact parent-generation linkage, atomic two-leg funded-margin reservation, idempotency, partial/failure handling and stop-preservation tests all pass together.

The study-only leverage path additionally needs real purged-heldout edge uncertainty and PIT account/market inputs; until then it correctly emits `best_leverage=null`. This is separate from the portfolio-aware paper allocator and from historical runtime fills that already used 1x–3x.

The new margin pass closes same-cycle overbooking under one paper-loop owner; it is not Redis-transactional and now says so explicitly (`cross_process_atomic=false`, `single_active_writer_required=true`). Source stamps matching standalone/embedded receipts with `generated_utc`. At 2026-07-17 18:33–18:45 UTC the standalone account-margin key reported a valid arithmetic snapshot but no timestamp, the simultaneous ledger did not embed the receipts, and all six open positions still lacked generation identity. That is retained as historical mixed-shape evidence. After the coordinated reload, the current margin snapshot was `PASS` with two open positions at **2x**, `used_margin_usd=55.8075` and `free_margin_usd=2929.7872`. This proves that current paper positions are not pinned to 1x and that the observed arithmetic snapshot is internally passing; it does not cure negative edge, authenticate the bracket account binding, prove cross-process reservation, or authorize higher leverage.

At 19:16:17 UTC the trainer also still emitted the pre-reload promotion shape: `checkpoint_promotion_allowed=true`, reason `VALIDATION_GUARD_DISABLED`, 403 validation rows, and a promoted candidate, while every new PIT-split/serving-edge field was absent. The update lane was outcome-supervised and PPO was false. This is explicit negative deployment evidence: the source mandatory PIT/edge gate was not yet controlling that cycle, and the promoted checkpoint cannot be retroactively certified by the later code.

A diagnostic inspection exposed a cloudflared credential in tool output. Its value is intentionally absent from every document. Rotate/revoke it and move tunnel authentication to protected credential handling.

### Final-admission, reservation and control-authority reconciliation (2026-07-18 UTC)

The latest source closes several paper-only proof gaps described earlier in this section. It does **not** close the live, profitability, A+, or cross-process-atomicity gaps. The operative candidate path is now:

```text
exact venue/filter + account/market/control snapshots
  -> fresh-current-mark open-book valuation and durable source-fill proof join
  -> paper_cycle_reservation_snapshot_v1
  -> adaptive allocation bound to the snapshot hash
  -> strict accepted-list-prefix commit check
  -> paper_cycle_reservation_commit_v1
  -> exact revocable-control reread
  -> paper_final_admission_contract_v3
  -> in-process accepted-list append
  -> persistence projection/intrinsic replay or quarantine
```

The contracts and authority boundaries are:

| Surface | Exact source owner and contract | Fail-closed behavior and blast radius |
|---|---|---|
| Same-cycle capacity | `services/paper_trade_management/cycle_reservation.py::{build_cycle_reservation_snapshot,build_candidate_commit_receipt}`; schemas `paper_cycle_reservation_snapshot_v1` and `paper_cycle_reservation_commit_v1` | Binds base-resource, precycle-exposure and dynamic-envelope hashes; subtracts prior accepted notional, same-symbol notional, allocated margin and stop loss; enforces total/symbol notional, margin buffer, per-candidate risk and projected drawdown before append. Any prior row without a replayable v3 final receipt, revocable receipt, allocation identity and intrinsic cycle receipt blocks the next candidate. |
| Append versus persistence replay | `cycle_reservation_prior_rows_rejection_reasons` compares the current accepted-list prefix at append; `intrinsic_candidate_commit_receipt_rejection_reasons` and `validate_intrinsic_candidate_commit_receipt` later replay only the immutable snapshot/allocation/commit chain | A row added, removed, reordered or mutated between snapshot and append blocks. Persistence does not incorrectly compare against a later mutable accepted list after lifecycle collapse/netting; it replays the row's intrinsic proof instead. |
| Current open exposure | `v2_trade_management_paper_loop.py::{_paper_accepted_fill_proof_source,_paper_precycle_current_mark_exposure_snapshot}` | Reads durable proofs only from `v2:paper:accepted_fills`, joins every open position to all stable source-fill IDs, requires each source fill to pass persisted v3 validation, requires exactly equal positive allocator `max_loss_if_stop_hit`/`max_loss_usd`, and values `abs(quantity) * fresh mark`. Missing/stale mark, missing proof, reused fill ID, legacy/unsealed open position or partial join blocks **new entries**; exit handling remains available. |
| Premium-index mark/index freshness | Producers `v2_binance_mark_price_wss_seeder.py`, `v2_binance_public_metadata_ingestor.py` and `v2_native_ingestors_live_loop.py`; unit `ai-bot-v2-binance-mark-price-wss-seeder.service`; consumer `_read_v2_mark_index_evidence`; primary key `v2:market:mark_price:<SYMBOL>` with funding/prices fallbacks | The former metadata/native loops could reread rows they wrote, reserialize roughly 23-hour-old Binance event data, refresh Redis TTL and omit `generated_at`/`available_at`. Producers now preserve source `event_time`, stamp `generated_at`, `available_at` and `expected_update_interval_seconds`, and reuse cache only when source-event age is at most 120 seconds; Redis TTL is never freshness evidence. The public `!markPrice@arr@1s` worker writes only the V2 mark keys with 180-second TTL, exits after 600 messages so systemd reconnects/re-resolves the adaptive universe, and carries no REST/account/order/leverage/margin mutation. One message covered 149/149 symbols; the persistent unit was active and T/BTC/ARB/MANA consumer rows were `CURRENT` at about 1.14–1.16 seconds age with exact clocks and one-second cadence. The consumer requires aware `event_time <= generated_at <= available_at <= observed_at`, positive mark/index and hash contract `REDIS_KEY_OUTER_SELECTED_PATH_PAYLOAD_AND_FIELD_MAP_SHA256_V2`. A controlled paper one-shot later built 597 blocked candidates, but its supplied proof does not enumerate every candidate's WSS receipt/reason set or accepted-fill propagation; cycle-level WSS closure remains pending. |
| Allocator market evidence | `_read_v2_microstructure_trust`, `_derive_allocator_liquidity_score`, `_derive_allocator_regime_score`, `_derive_candidate_correlation_contexts`, `_build_allocation_input` | Signal/prediction/feature liquidity, regime and correlation fields are diagnostic only. Trust rows require schema `microstructure_trust_score_v2`, exact symbol/timeframe and aware `generated_at <= available_at <= decision_time`; executable rows also require a finite trust score, finite adaptive minimum in `(0,1]` and action `ALLOW`/`REDUCE_SIZE`. Executable liquidity comes from the current microstructure score or the **complete pair** of observed orderbook depth and spread; either half alone or no pair returns zero. Regime requires an explicit intent/strategy-explanation score or intent-owned router labels/mode. Correlation reads explicitly final, available 1m candles—closed-source key first—and recomputes against **every** existing and same-cycle accepted other symbol; any missing/stale/unaligned required pair sets exposure to 1.0 and risk-vetoes. Naive ISO candle/decision clocks are rejected. Each child is schema `paper_correlation_accepted_source_material_v2` under `SOURCE_KEY_DECISION_TIME_ACCEPTED_CLOSE_AVAILABLE_FINALITY_AND_REJECT_COUNTS_CANONICAL_SHA256_V2`; the aggregate is `paper_correlation_aggregate_source_material_v1` under `DECISION_CANDIDATE_OPEN_SYMBOLS_AND_SORTED_CHILD_SOURCE_MATERIAL_CANONICAL_SHA256_V1`. Only no open positions or the same symbol alone is pair-safe at zero. Residual: the trust payload has no canonical `expires_at` and the consumer does not validate remaining Redis TTL, which prevents complete freshness/A+ proof. |
| Final mutable controls | `_paper_revocable_control_commit_revalidation`; schema `paper_revocable_control_commit_revalidation_v1` | Immediately before append it exact-rereads guardian, entry-freeze, portfolio, adaptive-tuning, paper-session, positions-or-ledger and closed-trade source material; recomposes effective freeze/current risk; checks guardian TTL in `[1,180]`; and revalidates the current process as the sole canonical paper writer. It also resolves the current paper-session identity and validates the canonical tuning envelope at both reread and commit clocks. A changed/malformed/missing/expired source, session mismatch, invalid tuning semantics or failed owner projection blocks. Portfolio-truth/nonoverridable freeze cannot be bypassed by a probation tier. |
| Final/persisted seal | `_paper_final_admission_point_in_time_contract`, `_paper_persisted_admission_projection`, `_paper_persisted_admission_rejection_reasons`; schema `paper_final_admission_contract_v3` | The final receipt binds component clocks, gate/decision records, allocation identity/economics, exchange-filter and maintenance-bracket rereads, revocable receipt, cycle snapshot/commit, safety flags and a hash of the row projection. Missing, v2-shaped, coherently rehashed-but-semantically-changed or post-seal-mutated rows fail persistence and are not clean evidence. |
| Adaptive gate tuning | Canonical CLI `v2_adaptive_gate_tuner.py` writes `v2:orchestrator:adaptive_gate_tuning_state`; current source schema/policy/receipt `v2_adaptive_gate_tuning_state_v4` / `v2_adaptive_gate_policy_v4` / `v2_adaptive_gate_tuning_receipt_v1`. Dormant `runtime_tuner.py` writes only `v2:diagnostic:adaptive_gate_tuning:runtime_tuner_shadow` with `authoritative=false`. Paper admission validates through `adaptive_gate_tuning_rejection_reasons` and `_paper_adaptive_tuning_semantic_validation`; schema `paper_adaptive_tuning_semantic_validation_v1`. | V4 adds exact canonical finalized-candle sources for BTCUSDT/ETHUSDT/SOLUSDT 1m. `learn_market_regime` requires 20 final rows per symbol, strict `close <= event <= available <= cutoff`, and a latest availability within three 60-second cadences. It learns q25/median/q75 and maps the current empirical percentile continuously into a bounded volatility factor 0.70–1.50; missing/untrusted market evidence makes combined authority fail closed. The existing 20-clean-outcome floor and truthful outcome/session/source/hash/clock checks remain. At 04:41:09.475673Z a canonical v4 receipt was published with 91/92 outcomes admitted, one `PREDICTION_LINEAGE_CONFLICT`, outcome/market evidence sufficient, NORMAL regime, factor 1.12666667, authority `CANONICAL_EVIDENCE_BACKED_RESTRICTIVE` and policy `EVIDENCE_BACKED_RESTRICTIVE_NONPOSITIVE_EDGE`. This is bounded producer/component proof; the held/failed paper reload produced no accepted-fill or final-reread propagation proof. Redis ACL/exclusive-writer enforcement remains unproved. |
| Bounded adaptive-sizing operator status | `_paper_adaptive_sizing_runtime_status`, `_paper_adaptive_sizing_operator_projection`, persistent/exploration compaction, `write_payload`; Redis `v2:paper:adaptive_sizing_runtime_status`; compact public file plus the top-level live status; schemas `paper_adaptive_sizing_operator_projection_v1`, `paper_candidate_allocation_operator_hash_contract_v1`, `paper_candidate_canonical_aggregate_contract_v1` | Controlled one-shot: exit 0/69.85s/4.70 GiB peak/zero swap, 597/0/597, 551 keys, resource classification `PRODUCTION_OK` only; canonical file 4,160,870 bytes and temp 5,017,701 with no non-finite tokens. Adaptive 622/622 hashable and Guardian-valid; current 0-source PASS; persistent 132/132 legacy-unhashable FAIL_CLOSED; exploration ACTIVE/all 597 blocked. Service remains held; repeat-service stability and historical quarantine remain open. |
| Counterfactual/resource containment | `counterfactual.py::run_counterfactual_sweep`; streamed feasible-axis/hedge accumulators; Guardian SQLite publisher/outbox/migration and `pit_prediction_counter.py`; `v2_microstructure_feed_quality_monitor.py::{_loop_log_payload,main}`; schema `v2_microstructure_feed_quality_monitor_loop_log_v1`; `agent_supervisor.py::{NON_DURABLE_POLL_EVENTS,append_event}`; `native_cuda_trainer_logrotate.conf`; timer `ai-bot-v2-trainer-logrotate.timer`; Redis Guardian/trainer keys; seven `99-codex-repair-hold.conf` runtime drop-ins | Current counterfactual source no longer retains cross-candidate feasible rows and rejects missing maintenance evidence. 323 tests plus 4,592,832-configuration real probe: 1.65 GiB peak RSS, zero swap, expected semantic NO_GO. The monitor formerly wrote its full 149-symbol rows to stdout at 679,324 bytes/4.62s (~12 GB/day); its service now uses `--loop-log-mode compact`, while full authority remains in Redis/status artifacts. Thirteen focused CLI tests passed. A paper-shadow/no-order-only restart was active as PID 364367; 949 bytes/4.95s and a 149-symbol scalar row demonstrated >99.8% lower byte rate. A 256 MiB trigger/four compressed rotations, checked every ten minutes, now covers persistent/offline/guard/monitor streams. About 14 GB of monitor history was gzip-preserved as ~876 MiB; no evidence was deleted. Redis remained ~25.49 GiB; its 6,054,941-row Guardian list (~6.23 GB) and 536,827,021-byte counterfactual string both had TTL −1. The 10.9 GB supervisor log was still growing because its ~0.9-GiB/day poll-event source repair was not deployed. Seven research/trainer/replay services are held inactive/dead; trainer timers are stopped. The original 62-test retention draft was rejected. Corrected source now passes 87 combined/18 focused consumer cases using transactional Guardian hot-delivery outbox, bounded legacy migration, explicit-cost immutable counterfactual archive/hot set, publisher-compatible IDs/hashes, strict UTC PIT/finality checks, dirty quarantine, fsynced crash-idempotent sink and a migration+outbox+cursor+chain trim gate. Runtime Redis is unchanged: no migration, trim, delete, reload or resident burn-in occurred. No live service was touched. Require a controlled versioned migration and repeated-service burn-in before removing holds. |
| Trainer liveness and exact-PPO semantic audit | `runtime.py` cycle/load/promotion path; `ppo_trainer.py::{_chronological_purged_split,_ppo_ineligibility_reason}`; confidence V2 target/feedback enrichment; on-policy receipt builder; train-tensor cache | **TRAINER NOT READY at runtime, despite integrated source mitigation.** The cycle distinguishes outcome-supervised cold-start bootstrap, non-serving candidate, rejected attempt and verified serving lineages; requires real optimizer mutation, PIT-safe positive after-cost one-SE LCB, fitted checkpoint-bound confidence with global/LONG/SHORT uncertainty non-regression, durable artifact verification and exact claim/fence/ledger disposition before serving. Terminal/trajectory/reward, strict candle-before-decision and full ordered cache identity now fail closed in source. Remaining paper/trainer blockers are sole/latest PPO validation-only starvation, old deployed cost evidence, process-local lane authority and no held cold-start→candidate/reject→promotion→restore/crash runtime proof. V2 target/calibration supply remains zero/unproved. Keep four trainer services held and both timers stopped until the final suite and bounded lifecycle/resource burn-in pass. |
| Paper cascade margin diagnostic | `v2_portfolio_cascade_guard_loop.py::_paper_margin_inputs` → `cross_margin_liquidation.py::build_portfolio_liquidation_snapshot`; schemas `paper_cascade_margin_join_v1`, `cross_margin_liquidation_v2` | Every open symbol must join exactly one valid `paper_account_margin_status.position_margin_rows` row with valid maintenance evidence, positive notional/rate and leverage >=1; missing/duplicate/invalid rows block portfolio computation and emit `UNTRUSTED_MARGIN_EVIDENCE`. Quantity aliases are `positionAmt/position_amt/net_quantity/qty/quantity`; mark aliases prefer mark/current/last estimates before entry fallback; leverage aliases are `leverage/effective_leverage/recommended_leverage`. The 04:40:17.785Z component receipt joined 2/2 and was complete/no-breach, but no completed paper cycle consumed it. The pure engine reports leverage completeness and cannot mutate margin. Its fixed BTC shocks (−5%, −10%, −20%, +10%) and betas (BTC 1.0, ETH 1.15, SOL 1.35, other 1.6) are diagnostic model debt, not adaptive cross-margin authority. |
| PPO behavior-supply and trainer-generation audit | Historical paper `_entry_is_exact_on_policy_sample`; current adaptive receipt/finalized outcome; `training_state.py`; checkpoint lifecycle; trainer runtime | Runtime truth remains old-generation 0 admitted/0 consumed with 2,014 missing-field rejections. Current source requires strict clocks, exact weight/evidence/symbol/timeframe and hashed adaptive cost; terminal reward/outcome digest; full ordered tensor identity; immutable no-expiry receipt; semantic SQLite claim/fence/ledger; distinct serving/candidate/rejected stores; startup reconciliation and verified promotion/restore. The deployed cost producer still emits the old schema and no held integrated cycle ran. Source tests cannot supersede runtime 0/0 or grant A+/live. |
| Ridge model-edge challenger | `native_trainer/model_edge_recovery_challenger.py`; schema/policy v2; durable archive and champion/challenger status | Paper-only B-grade/no-fill/no-A+/no-live lane now has fail-closed PIT/cost/split/holdout mechanics and 16 focused tests. Real probe: 200 scanned, 145 PIT/finality/hash admitted but all missing fee+slippage, 55 missing latest-unclosed proof, 0 evaluable. No model/edge/paper permission and no +30-bps claim. Fixed model/grid/supply/bootstrap defaults remain threshold-classification debt. |
| Operator-approved per-symbol leverage and binding integration | `symbol_leverage_ceiling` / `_liquidation_safe_max_leverage`; `dynamic_envelope.py`; allocator `_adaptive_leverage_target`; `_paper_allocate_with_bracket_evidence`; authenticated bracket and margin contracts | Preserve the implemented 75/50/20 ceilings and five-ATR rule. Source/test proof covers favorable PIT interpolation from base toward the authorized ceiling, adverse/missing-evidence contraction, target `min(Phase-8 recommendation, continuous target within dynamic/symbol cap)`, and integer bracket ladder through `min(signed bracket max, tier)`. Runtime must still prove the conservative account-specific bracket/PIT market/account minimum and `notional = margin * leverage`; negative edge, blocked credential binding and isolated-only execution currently prevent high leverage from being earned. |
| Truthful close-outcome availability | `build_close_event`, lifecycle `_close_position`, `capture_close_outcome_availability`; schema `PAPER_CLOSE_OUTCOME_AVAILABILITY_V1` | Economics first stamps `close_event_time` and `outcome_generated_at` without claiming availability. Immediately before publication, lifecycle seals aware UTC `outcome_available_at` only when `close_event_time <= outcome_generated_at <= outcome_available_at`; a naive or impossible clock blocks the seal and leaves the position retained. Entry-feature `available_at` remains a different field, and historical rows are not backfilled. |
| Exact exchange filters | `v2_direct_orderbook_recorder.py::{_safe_symbol_filter_cache_set,_validated_canonical_symbol_filter_cache_payload,_canonical_symbol_filter_cache_refresh_due,fetch_provider_symbol_support}` owns exact `v2:exchange:symbol_filters:<SYMBOL>` rows | Public `GET /fapi/v1/exchangeInfo` is an explicitly enabled REST fallback for metadata only; the row binds source payload hash, writer, schema, fetched/ingested/available clocks, 24-hour Redis TTL and a 15-minute refresh cadence. The paper consumer requires the exact symbol, `TRADING` USD-M perpetual/USDT contract and exact `MARKET_LOT_SIZE` plus `MIN_NOTIONAL`, then rereads it at final admission. Initial BTCUSDT/ETHUSDT/SOLUSDT key production was observed; advancement on the periodic refresh cadence remains runtime proof debt. |

No receipt above claims a distributed transaction. The revocable receipt explicitly records `cross_process_atomic=false` and the residual window `CONTROL_OR_OWNER_CAN_CHANGE_AFTER_FINAL_REREAD_BEFORE_IN_PROCESS_LIST_APPEND`. The cycle prefix comparison protects sequential work inside one owner; it is not a Redis fencing token, CAS, durable reservation journal or multi-key transaction. A second process can still race snapshot → validation → append/publish. Until a durable generation fence exists, “one observed writer” is a required operating assumption, not A+ proof.

Function-level change impact for the new chain:

| If this function/key/schema changes | Revalidate at minimum |
|---|---|
| `_paper_precycle_current_mark_exposure_snapshot` or `v2:paper:accepted_fills` | open-position/source-fill identity, current-mark hashes/times, equal max-loss aliases, projected drawdown input, legacy-position fail-closed behavior, exits, ledger compaction and persistence quarantine |
| `v2_binance_mark_price_wss_seeder.py`, either premium-index cache consumer/republisher, `_read_v2_mark_index_evidence`, or `v2:market:mark_price:<SYMBOL>` | source-event age rather than Redis TTL, all three event/generated/available clocks, expected cadence, canonical-key priority and fallbacks, mark/index positivity, V2 source-material hash, candidate-time all-symbol coverage, precycle current-mark valuation, divergence stress input and fail-closed reasons |
| `_read_v2_microstructure_trust`, `_derive_allocator_liquidity_score`, `_derive_allocator_regime_score` or `_derive_candidate_correlation_contexts` | trust schema/symbol/timeframe/clocks/TTL debt, adaptive minimum/action, complete depth+spread adapter, intent-owned strategy regime, strict candle clocks/finality/availability, child `paper_correlation_accepted_source_material_v2` hash contract, aggregate `paper_correlation_aggregate_source_material_v1` hash contract, closed→generic source fallback, all existing/same-cycle symbol pairs, `allocator_market_evidence_status`, risk veto, dynamic leverage/risk/notional and exploration tiers |
| `build_cycle_reservation_snapshot` or `paper_cycle_reservation_snapshot_v1` | allocator `available_margin`, symbol/total exposure, `paper_risk_budget_fraction`, dynamic-envelope limits, prior-row order, emergency cap, allocation lineage hash and every commit replay |
| `build_candidate_commit_receipt` or `paper_cycle_reservation_commit_v1` | accepted-prefix equality, allocation economic aliases, all five capacity checks, final v3 bound material, intrinsic persistence replay and later same-cycle collapse/netting |
| `_paper_revocable_control_commit_revalidation` | the seven Redis source roles including paper session, session identity, tuning semantic validation at reread/commit clocks, guardian TTL, freeze composition, current-risk derivation, procfs/systemd owner projection, final clock ordering, persistence projection and quarantine rules |
| `_paper_final_admission_point_in_time_contract` or `paper_final_admission_contract_v3` | every producer of a component clock/hash, risk/orchestrator decisions, exchange filters, bracket evidence, allocator receipts, cycle receipts, revocable receipt, lifecycle copying and accepted-fill persistence |
| `v2:orchestrator:adaptive_gate_tuning_state` | canonical producer/schema/policy/receipt, source/session hashes, `outcome_available_at <= outcomes_cutoff <= generated_at <= available_at < expires_at`, entry/preemptive consumers, revocable exact reread and shadow isolation |
| `learn_market_regime`, any canonical 1m candle key, or tuner v4 | finality flags; `close <= event <= available <= cutoff`; 20-row/source integrity floor; three-cadence staleness bound; empirical q25/median/q75/percentile; 0.70–1.50 bounded factor; combined fail-closed authority; source manifest/policy/hash/receipt; v4 paper-consumer compatibility |
| `_paper_adaptive_sizing_runtime_status`, its two operator schemas, Redis status key or public JSON | full-row canonicalization and source order; per-row and aggregate hashes; max-five projection bound; omitted-field/truncation markers; exact all-candidate aggregate contract; OOS context-only rejection; guardian tamper/missing/sixth-row failures; file/Redis bytes, RSS and write-rate after reload |
| `_paper_margin_inputs` or `cross_margin_liquidation_v2` | symbol uniqueness/join completeness; canonical margin-status PASS/accounting; positive rate/notional; leverage >=1 and alias coverage; maintenance/leverage completeness; fail-closed portfolio suppression; static shock/beta calibration; isolated-paper execution invariant |
| `on_policy_behavior.py`, `durable_behavior_receipt_archive.py`, `_entry_is_exact_on_policy_sample`, `BEHAVIOR_POLICY_LINEAGE_FIELDS`, `training_state.py`, checkpoint roles or any exploration sampler | adaptive-plan input/hash/carry/ordinary-lane accounting and restart/fleet fencing; strict `close<=cutoff<=available<decision` plus `close<decision`; required weight/evidence/symbol/timeframe identity; masked raw-logit sample/U53/probability/log-probability/value; complete hashed order-book cost payload, adaptive expiry, configured paper fee/notional evidence and fallback exclusion; actual exchange-account fee authority before live transfer; immutable archive/no-expiry Redis receipt; ordered PUBLISHED/ENTRY/OUTCOME/TRAINER lifecycle and post-ledger consumption hook; archive retention/GC/backup; exact finalized-outcome/reward binding; ordered full-tensor cache digest; claim/WAL fence/ledger partition; candidate/rejected/serving lineage; crash/reject/restore/reconcile; purged after-cost forward evidence |
| `symbol_leverage_ceiling`, `PAPER_MAX_LEVERAGE`, dynamic-envelope interpolation, allocator symbol clamp, authenticated integer leverage ladder, ATR safety or any leverage env | authenticated account/environment bracket maximum; total post-fill notional; PIT volatility/edge/liquidity/correlation/cascade; liquidation/maintenance/account/free-margin evidence; G10 identity; isolated mode; adverse/missing-evidence contraction; static-threshold inventory |
| `v2:exchange:symbol_filters:<SYMBOL>` | recorder refresh/TTL, raw `filters[]` uniqueness, MARKET quantity quantization, minimum notional, final reread, post-quantization notional/margin/leverage identity and paper-only safety flags |

The current operational decision remains **NO-GO** and **TRAINER NOT READY**. The last completed promotion evidence still reported `HALTED_PERFORMANCE`, after-cost LCB **−47.0423 bps**, PF **0.703666**, notional-weighted expectancy **−7.70099 bps**, and win rate **0.43478**. Canonical tuner v4 has bounded component proof—91/92 admitted outcomes, NORMAL factor 1.12666667, restrictive nonpositive-edge policy—but no accepted-fill propagation. The controlled paper one-shot completed 597/0/597 at 4.70 GiB peak RSS and zero swap with valid current/adaptive hashes, while persistent legacy evidence stayed FAIL_CLOSED. The controlled Guardian one-shot semantically exited 2 in 3.57 seconds at 35,896 KB peak RSS/zero swap: anti-metric-gaming and the canonical aggregate contract passed, but it kept `A_GRADE_HALTED_PERFORMANCE`, allowed only reduce/close/emergency de-risk, recommended all 26 rows at 1x and admitted 0 economic holdout rows despite 99,644 PIT-valid coverage observations. These are bounded one-shots, not resident burn-in: seven research/trainer/replay services remain explicitly held and both trainer timers are stopped. Redis remained ~25.49 GiB with two unexpired large evidence objects. Corrected retention source passes 87/18 but no runtime migration, trim or reload occurred. The resident microstructure monitor's full-row stdout amplification is repaired (>99.8% lower sampled byte rate), and compressed rotation preserved its roughly 14 GB history in ~876 MiB, but the 10.9 GB supervisor event log/temp files remain; its source repair was not deployed and one short monitor sample is not host-stability closure. Account margin passed with two positions fully accounted—equity/wallet $2,985.59472051, used $55.80754736, free $2,929.78717315, adaptive buffer $499.52893144 and post-buffer free $2,430.25824171—but bracket security remained `BLOCKED:CREDENTIAL_BINDING_NOT_ACCOUNT_SPECIFIC`; no leverage binding, leverage/margin mutation or order occurred. Every candidate's WSS receipt is also unproved. Ridge retained zero trusted rows. Exact trainer lifecycle, ledger, reward, terminal, clock, cache, checkpoint-path and calibration gates are now source-integrated, and receipts no longer expire; configured paper/shadow fee and reference-notional identity is now bound and rederived, but the deployed cost producer remains old and no held integrated cycle ran. Runtime stays PPO 0/0; sole-row split starvation, process/fleet lane state, static thresholds and controlled lifecycle/crash/burn-in proof remain. Actual exchange-account fee-tier/discount/maker-taker authority is still required before live transfer. G11/G12 failed. A+/1000x claims are prohibited; live remains disabled.

## Verified scale

- 9,272 tracked paths in the content-revalidated atlas snapshot.
- 3,213 parsed Python modules; 32,272 Python symbols.
- 3,334 TypeScript/JavaScript and 693 Swift symbols.
- 25,389 Python import edges and 161,112 call references retained.
- 1,807 declared data/schema contracts and 39,538 field names.
- 2,918 environment-key names, 2,040 Redis patterns and 905 API definition/reference records.
- 157 installed `ai-bot*` user units at the earlier operations snapshot; 81 services running, 36 active timers and 3 failed services. The direct recheck found 156 installed basenames and 35 active timers, confirming deployed topology is mutable.
- Current OpenAPI snapshot: 189 paths/193 HTTP operations, plus seven mounted WebSocket paths outside OpenAPI.
- Redis snapshot: about 1.11 million keys and 31 GiB against a 32 GiB `allkeys-lru` limit, RDB-only/AOF disabled.
- Native model ABI still has deployment/history compatibility debt: the 2026-07-16 deployed audit measured 477 ordered features × four segments = 1,908 inputs, while current source intentionally resolves 446 × four = 1,784 after 31 provider features were removed. The three stale tests were reconciled to the current contract and the combined native-trainer/pipeline-trust suite passed 374 tests. That local green suite does not version the ordered schema across deployed checkpoints, caches, temporal buffers, and archived replay; those surfaces still require digest-bound migration/quarantine before treating 446/1,784 as universally compatible.

## Post-cut reconciliation (2026-07-16, evening)

The cut above is a point-in-time snapshot. The deltas below were directly re-measured later the same day, after operational hardening and web/mobile work in this session. The audit decision is unchanged: **NO-GO for live; LIVE TRADING BLOCKED.** The paper trade loop (`v2_trade_management_paper_loop.py`) is owned by a separate agent and was not modified here.

**Re-measured scale** (`systemctl --user list-units 'ai-bot*'`, `redis-cli DBSIZE`/`INFO memory`/`CONFIG GET save`, `/openapi.json`):

- Installed `ai-bot*` user units: **159** (was 156/157). Running services: **84** (was 81). Active timers: **36**. Failed services: **2** — `ai-bot-v2-autonomous-no-manual-next-task-policy` and `ai-bot-v2-closed-candle-replay-evidence` (was 3).
- Redis: **941,651 keys / 31.50 GiB** against the 32 GiB `allkeys-lru` cap; `save "900 1"`, `appendonly no` (RDB-only confirmed). Key count fell from ~1.11M as LRU eviction ran under sustained memory pressure.
- OpenAPI: **189 paths / 193 operations — unchanged.** The derivatives work added `/api/v2/derivatives` and `/api/v2/market/{symbol}/derivatives` as enrichment of the existing surface; net path/operation count did not move.

**Operational hardening applied this session** (effective systemd drop-ins, not tracked in git — verify with `systemctl --user show`):

- `ai-bot-v2-out-of-sample-evidence-producer` — **removed** (stopped + disabled). It was in an OOM-restart loop (~4.2 GiB per restart) and a primary contributor to the workstation OOM event.
- `ai-bot-v2-adaptive-capital-productivity` — **memory-capped** via drop-in `MemoryHigh=6G / MemoryMax=8G` (previously uncapped; leaked to ~15.5 GiB loading millions of counterfactual configs).
- `ai-bot-v2-paper-equity-reconciliation-loop` — **`StandardOutput=null`** drop-in (had flooded `/var/log/syslog` at ~600 lines/s to ~35 GiB, starving disk I/O).
- New operator scripts: `tools/OPERATOR_crash_hardening_sudo.sh` (truncate the 35 GiB syslog + cap journald to `SystemMaxUse=2G`; **needs sudo — pending operator**) and `tools/fix_cursor_state_bloat.sh` (reclaim Cursor's ~18 GiB `state.vscdb` AI-history cache behind the "Codex loading" hang; **run with Cursor closed — pending operator**).

**Still open (unchanged by this session):**

- The retention conflict in §"Deployment/operations truth" persists: `ai-bot-v2-orderbook-replay-rollover.timer` is still enabled/active and the 15-minute `ai-bot-v2-disk-retention-janitor.timer` still runs without `--dry-run`. Repairing the rollover would let its timer invoke the harsher policy — treat as an operator decision, not an automatic repair.
- Persistent/offline trainers and duplicate portfolio publishers remain concurrently active (`continuous-offline-gpu-trainer`, `native-cuda-trainer-persistent`, `trainer-training-live-loop`, `trainer-checkpoint-evidence`; `portfolio-state-publisher` + `portfolio-cascade-guard`).
- Redis remains near its cap with no discovered HA/tested restore.

**Web/mobile changes this session** (source edits, deployed via controlled build): derivatives + markets pages rebuilt for full real-time coverage; AI page trainer telemetry expanded; natural-language signal reasoning; a reusable NERVYX chart library (Recharts) across dashboard/portfolio/AI/signals/derivatives; matching Path-based SwiftUI charts on iOS dashboard/positions with backend `equity_curve`/`win_rate` payloads.

## Architecture

```text
exchange/provider readers
  → Redis market/provider truth
  → closed-candle/native feature/TA/context snapshot
  → ordered `FEATURE_SPEC` × values/missing/stale/availability tensor
      (audit-time deployment 477×4; current worktree 446×4; drift unresolved)
  → native policy/trainer/checkpoint
  → prediction/replay/archive publication
  → all-timeframe publisher
  → orchestrator proposal
       ├─→ risk ALLOW/DENY record
       └─→ paper signal
  → paper trade-management/lifecycle/accounting
  → positions/portfolio/guardian/outcome/replay/feedback

FastAPI REST/WebSocket/static → React/Vite + SwiftUI/watch/CLI

Dormant guarded branch → Binance WebSocket order.place
```

## Temporal contract

The following are never interchangeable:

- `event_time`: source event occurred;
- `ingested_at`: system received/persisted it;
- `available_at`: exact value became safe to use;
- `generated_at`: derived record was computed;
- `feature_cutoff`: newest information represented;
- `decision_time`: policy decision was fixed;
- `execution_time`: paper/live materialization occurred.

All contributing availability and final candle close times must be no later than decision time; MASA cutoff must be no later than PPO decision time; decision must be no later than execution. The canonical candle/trust modules enforce much of this. Current external feature enrichment does not preserve/check every contributing source envelope, so end-to-end point-in-time proof is incomplete.

## Current model/training truth

The native network is a residual PyTorch policy with optional attention/GRU and policy/value/expected-move/action-conditioned-confidence/MASA heads. Active config used larger width/depth than source defaults. Inference selects only hold/long/short from the seven-action head. The confidence head now has two outputs, LONG and SHORT, with semantics `P(selected directional action has recomputed net USD > 0 under explicit unit-qualified gross/fees/slippage/signed-funding and close-specific notional)`. HOLD is deliberately zero/unfitted. Policy action probability and expected-move magnitude are not profitability confidence. MASA is one learned auxiliary scalar blended with a deterministic adapter, not a multi-agent system.

The trainer is a hybrid PPO-shaped/supervised implementation. The deployed/observed old generation records deterministic `DETERMINISTIC_ARGMAX_ALIGNMENT` / `EXPECTED_MOVE_ALIGNED_POLICY_V1` rows and historically expected `RAW_LOGITS_SOFTMAX_V1`; those rows fail PPO eligibility and can use only valid outcome supervision. The verified runtime result is 0 trainer-admitted and 0 consumed PPO rows, 2,014 missing-field rejections and a paper-owner fingerprint that is not a served checkpoint/model hash. Current undeployed source instead admits only the exact `v2_positive_edge_on_policy_behavior_receipt_v1` / `POSITIVE_EDGE_MASKED_RAW_LOGITS_SOFTMAX_V1` contract produced by the adaptive paper-only sampler. It validates a required checkpoint SHA/evidence digest, symbol/timeframe, strict PIT clocks, a self-hashed complete adaptive-cost publication, sampled action/mask/probability/value, exact finalized close economics, terminal trajectory and reward identity. A full ordered tensor-cache digest covers all rows, features, masks, targets, clocks and trust material. `training_state.py` defines candidate progress, confidence promotion, semantic checkpoint roles and an exactly keyed append-only consumption ledger with crash fencing; `run_hybrid_trainer_cycle` now invokes those controls, persists candidate/rejected/serving artifacts and restores only semantically verified serving state. Runtime genuine PPO nevertheless remains inactive because the held service has not executed this generation; no deployed candidate/serving or unique-consumption claim follows until a controlled lifecycle run proves it.

The adaptive-mode-collapse workstream's final read-only handoff confirms the still-running generation is old code, not the source repair: 745 predictions/0 publication failures, 2,014 outcome-supervised rows, PPO admitted/consumed/clipped rows 0/0/0 and `ppo_objective_used=false`. Its serving-policy validation edge was −1.39286013 bps with lower bound −2.25532918 bps; the composed gate remained HALTED. Paper margin was PASS and MANA/ARB were each represented at 2x. These values were handoff-reported rather than independently root-reproduced; they prove neither deployed on-policy sampling nor profitable leverage. Fresh controlled probes remain required after source acceptance.

Training evidence has current blockers: external temporal lineage gaps, remaining masked/missing-trust exceptions, globally enforced final-holdout isolation, exact identity in historical rows, the missing post-ledger `TRAINER_CONSUMED` archive event, unbounded behavior-archive retention and deployed failure-injection proof for archive/replay suppression. Actual exchange-account fee authority remains a live-transfer boundary, not a configured paper-training blocker. Current source uses strict aware parsers in enrichment/loading/windowing/PPO chronology; recomputes gross and every close-cost component; rejects fallback/contradictory outcomes; requires terminal/reward identity; and binds ordered tensors, adaptive cost evidence, checkpoint state, candidate progress and confidence uncertainty. Checkpoint artifact loading is confined to the exact manager-owned NPZ path, and dead post-optimizer ambiguity is consumed fail closed rather than replayed. The persistent service validation guard is enabled; offline child failures propagate; the main cycle integrates the candidate/ledger lifecycle. Historical replay/checkpoints remain untrusted or incompatible until versioned/regenerated, and all four trainer services remain held. No controlled burn-in, integrated unique optimizer update, compatible promoted serving checkpoint or deployed prediction proves these trainer changes yet.

## Current decision/execution truth

The two paragraphs below describe the 2026-07-16 audited source. The 2026-07-17 worktree mitigates the ordinary A+ risk-authority defect and retires the effective confidence fast paths as described in the dated reconciliation above. Do not treat either state as runtime-certified until fresh post-reload rows pass the new invariants.

The orchestrator creates a provisional risk ID and paper flag before gateway evaluation. The gateway may write a real `DENY`; paper dereference records it, but ordinary A-grade admission tests ID existence plus local pre-trade rather than requiring action `ALLOW`.

The active paper loop also contains confidence overrides. At confidence ≥0.65 a fast path accepts and continues before tier/upstream enforcement, direction, sizing, churn, portfolio freeze, preemptive admission, fill invariant, accounting annotations and PPO entry stamping. Higher thresholds relax additional gates. One fee override writes a frozen dataclass and causes runtime exceptions.

Therefore current paper fills are research artifacts until individually classified by risk action, admission path, temporal completeness, invariant/position state, label/cost version, durability and PPO-field completeness.

## Live boundary

No active authorized real submitter was observed; release mode was effectively non-live and live gate disarmed. Real Binance order transport and dormant callers exist, including a runtime whose transport call defaults non-dry-run. Any edit/enablement of live/order/cancel/modify behavior requires explicit operator approval and a new safety audit.

## Deployment/operations truth

- Effective backend: four Uvicorn workers, `127.0.0.1:8000`, mutable repo source, stacked user-systemd drop-ins overriding an older release symlink.
- Effective frontend: Vite preview, `0.0.0.0:5173`, ignored prebuilt `dist`; source edits are not deployed until a controlled build.
- Installed and versioned unit sets diverge. Four trainer definitions—persistent CUDA, continuous offline GPU, scheduled pretrain and native PPO/MASA continuous guard—now quote their complete path-with-spaces `PYTHONPATH` in installed and `claude_worklog/systemd/user` mirrors; targeted verification found no diagnostic attributable to those four and effective environments preserve the full path. Four other installed units still have the old unquoted defect, and some services mask child failures.
- Persistent/offline trainers and duplicate portfolio publishers were concurrently active at the baseline cut. The exact trainer services `ai-bot-v2-native-cuda-trainer-persistent`, `ai-bot-v2-continuous-offline-gpu-trainer`, `ai-bot-v2-trainer-scheduled-pretrain` and `ai-bot-v2-native-ppo-masa-continuous-training-guard` are now inactive/dead under `RefuseManualStart=yes` repair holds. The scheduled-pretrain and continuous-guard timers are stopped. This prevents an unreviewed auto-promote/restart or minute-loop guard attempt while repairs are tested; it is not a successful trainer burn-in.
- Two destructive retention policies conflict. At the `2026-07-16T08:32:41Z` observation, the failed 100 GiB rollover still had an enabled active persistent six-hour timer, while the 15-minute 300 GiB/five-day janitor was already running without `--dry-run`. Its `08:27:21Z` status recorded ten temporary holdout files deleted, so evidence preservation was actively racing retention automation; repairing the rollover would let its existing timer invoke the harsher policy.
- Redis is near its limit without discovered HA/tested restore.
- The central application/paper ORM and Alembic plane is uninitialized, while optional user/revocation/alert/trader-account SQL repositories are implemented but can create tables outside Alembic. The observed deployment used local JSON, Redis, files and one separate closed-loop SQLite WAL database instead.
- Auth/security middleware is mostly pass-through; route dependencies protect some handlers; four workers can race on local JSON state.
- Tunnel credential handling is unsafe and provider-side routing is not captured locally.
- Backend/frontend CI and dependency manifests are incomplete; full tests are unsafe against current live-like workspace state.

## Canonical documents

- [Full master audit](system_audit_2026_master/AI_BOT_V2_FULL_REBUILD_MASTER_AUDIT_REPORT.md)
- [Operator manual](system_audit_2026_master/AI_BOT_V2_MASTER_OPERATOR_MANUAL.md)
- [Technical reference](../v2/docs/V2_SYSTEM_TECHNICAL_REFERENCE.md)
- [Findings/risk register](system_audit_2026_master/CURRENT_FINDINGS_AND_RISK_REGISTER.md)
- [2026-07-17 adaptive end-to-end control/accounting map](system_audit_2026_master/ADAPTIVE_END_TO_END_CONTROL_AND_ACCOUNTING_2026-07-17.md)
- [2026-07-17 operator validation/monitoring runbook](system_audit_2026_master/OPERATOR_VALIDATION_AND_MONITORING_RUNBOOK_2026-07-17.md)
- [2026-07-17 exact command ledger](system_audit_2026_master/COMMAND_LEDGER_2026-07-17.md)
- [Validation and limitations](system_audit_2026_master/VALIDATION_AND_LIMITATIONS_2026-07-16.md)
- [Historical artifact classification](system_audit_2026_master/HISTORICAL_ARTIFACT_CLASSIFICATION.md)
- [Rebuild blueprint](system_audit_2026_master/REBUILD_BLUEPRINT.md)
- [Runtime/deployment internals](system_audit_2026_master/components/RUNTIME_PROCESS_AND_DEPLOYMENT.md)
- [Temporal data/features](system_audit_2026_master/components/DATA_TEMPORAL_LINEAGE_AND_FEATURES.md)
- [Trainer/PPO/MASA/replay/checkpoints](system_audit_2026_master/components/TRAINER_PPO_MASA_REPLAY_AND_CHECKPOINTS.md)
- [Decision/risk/paper/live execution](system_audit_2026_master/components/DECISION_RISK_PAPER_AND_LIVE_EXECUTION.md)
- [API/auth/storage/web/mobile](system_audit_2026_master/components/API_AUTH_STORAGE_WEB_AND_MOBILE.md)
- [Config/contracts/change impact](system_audit_2026_master/components/CONFIG_KEYS_CONTRACTS_AND_CHANGE_IMPACT.md)
- [Function-level atlas summary](system_audit_2026_master/atlas/ATLAS_SUMMARY.md)
- [One-row-per-module index](system_audit_2026_master/atlas/MODULE_BY_MODULE_INDEX.md)
- [Exact audit command/tool ledger](system_audit_2026_master/COMMANDS_RUN.md)

## Change protocol

Before any change:

1. find the exact symbol/key/field/config/route/unit in the atlas;
2. read direct and recursive callers/importers plus dynamic state consumers;
3. identify temporal, model, risk, position, persistence and client implications;
4. compare effective installed units/drop-ins, not only tracked files;
5. isolate tests from current Redis/paper/auth/runtime state;
6. preserve evidence and rollback;
7. obtain explicit approval for strategy, PPO, MASA, risk, live execution or destructive retention;
8. regenerate the atlas and update docs after implementation.

The full machine-level answer to “what will this small function affect?” is `docs/system_audit_2026_master/atlas/CHANGE_IMPACT_INDEX.json`, joined with the Redis/config/data/API/entrypoint registries and the effective runtime snapshot.

## Documentation changelog

- **2026-07-18:** recorded strict Ridge zero-row runtime proof; action-conditioned, train-only, checkpoint-bound profitability confidence plus V2 recomputed USD economics/V1 invalidation and unit-explicit outcome→feedback→loader propagation with runtime supply still unproved; exact on-policy receipt source repair with runtime still 0/0; retention-manifest and runtime-truth repairs; validation-guard enforcement and offline failure propagation; bounded paper/Guardian probes; exact seven-service/two-timer repair holds; unexpired Redis evidence pressure; the rejected first retention draft and corrected 87/18 source-only contract; adversarial trainer NOT READY findings for cold-start liveness, durable PPO consumption, split supply, promotion, terminal/clock and cache identity; and the undeployed supervisor poll-amplification fix. Trainer repair/burn-in, controlled Redis migration, A+ and 1000x remain pending/unproven.
- **2026-07-17:** added the adaptive-control/accounting contract, exact command ledger, operator validation runbook, strict A+ semantics, runtime negative-evidence snapshot, function-level impact matrix, source mitigations for PIT/purged validation/promotion/replay/PPO action and sampling identity/non-authoritative trainer proposals/rejected-candidate serving suppression/risk alias/durable publication/paper capital and cycle-local margin reservation, their remaining authority/economic blockers, and the security rotation action. Live execution remains unchanged and disarmed.
- **2026-07-16:** published the full reconstruction, static atlas, component internals, operator manual and rebuild blueprint; later that day recorded deployment/operations reconciliation.
