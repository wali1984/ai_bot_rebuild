#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR="$(CDPATH= cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
export PYTHONPATH="${SCRIPT_DIR}/v2/backend:${PYTHONPATH:-}"
exec python -m app.cli.run_pass3a_live_canary_safety_dry_run "$@"
