{
  "task_id": "093_codex_recovery_2e1d_end_file_marker_leakage_cleanup",
  "agent": "codex",
  "risk_level": "L1",
  "status": "pending",
  "depends_on": [],
  "predecessor_task_ids": [],
  "predecessor_required_marker": null,
  "predecessor_required_marker_file": null,
  "cwd": "/home/wali/Desktop/AI BOT REBUILD",
  "emit_files": true,
  "task_granularity_mode": "consolidated_default",
  "allowed_output_prefixes": [
    "claude_worklog/agent_supervisor/tasks/",
    "claude_worklog/phase2_core_rebuild/trainer_gpu_parity_impl/",
    "claude_worklog/agent_supervisor_reliability/",
    "claude_worklog/tools/"
  ],
  "forbidden_output_paths": [
    "v2/",
    "claude_worklog/autonomous_control_plane/",
    "claude_worklog/security/",
    "claude_worklog/requirements_inbox/"
  ],
  "required_output_files": [
    "claude_worklog/agent_supervisor_reliability/86_END_FILE_MARKER_LEAKAGE_RECOVERY_REPORT.md",
    "claude_worklog/agent_supervisor_reliability/86_END_FILE_MARKER_LEAKAGE_RECOVERY_GO_NO_GO.md"
  ],
  "prompt": "You are Codex acting as autonomous recovery agent under REQ_0015 (Planner-Level Human Attention Codex Autorecovery). Trigger condition: the prior planner turn emitted six files with a trailing standalone 'END_FILE: <path>' marker line that the materializer failed to strip; two of the six files are JSON task definitions that the supervisor cannot parse, blocking dispatch of milestone 2E1.D.\n\nYou do NOT modify /home/wali/Desktop/AI BOT. You do NOT modify any v2/ source or test file. You do NOT modify any prior-milestone trainer-parity source or test file under v2/backend/app/services/trainer_parity/, v2/backend/app/domain/trainer_liveness*, v2/backend/app/domain/liveness_stream_growth, v2/backend/app/domain/trainer_liveness_observation_collector, v2/backend/app/domain/trainer_liveness_composition, or v2/backend/app/adapters/redis_v2/. You do NOT write or read Redis. You do NOT restart any live service. You do NOT place, modify, or cancel any exchange order. You do NOT change leverage or margin. You do NOT enable live trading. You do NOT deploy. You do NOT run any production migration. You do NOT expose or commit secrets.\n\nScope of allowed edits in this task:\n- claude_worklog/agent_supervisor/tasks/091_trainer_parity_2e1d_service_composition_implementation.json (strip trailing END_FILE: <path> line only)\n- claude_worklog/agent_supervisor/tasks/092_trainer_parity_2e1d_service_composition_codex_review.json (strip trailing END_FILE: <path> line only)\n- claude_worklog/phase2_core_rebuild/trainer_gpu_parity_impl/112_PHASE_2E1D_SERVICE_COMPOSITION_SPEC.md (strip trailing END_FILE: <path> line only)\n- claude_worklog/phase2_core_rebuild/trainer_gpu_parity_impl/113_PHASE_2E1D_SERVICE_COMPOSITION_TEST_PLAN.md (strip trailing END_FILE: <path> line only)\n- claude_worklog/phase2_core_rebuild/trainer_gpu_parity_impl/114_PHASE_2E1D_SERVICE_COMPOSITION_SAFETY_BOUNDARIES.md (strip trailing END_FILE: <path> line only)\n- claude_worklog/phase2_core_rebuild/trainer_gpu_parity_impl/115_PHASE_2E1D_SERVICE_COMPOSITION_GO_NO_GO_REQUEST.md (strip trailing END_FILE: <path> line only)\n- claude_worklog/tools/claude_master_rebuild_planner.py (extend parse_begin_file_blocks regex/fallback to tolerate END_FILE and END_FILE: <path> identically; no other behavior change)\n- claude_worklog/agent_supervisor_reliability/86_END_FILE_MARKER_LEAKAGE_RECOVERY_REPORT.md (new report)\n- claude_worklog/agent_supervisor_reliability/86_END_FILE_MARKER_LEAKAGE_RECOVERY_GO_NO_GO.md (new marker)\n\nMandatory step sequence:\n\n1. Snapshot. For each of the six target files, read the absolute last line. If the last line matches exactly the regex r'^END_FILE:\\s*<repo-relative-path-of-the-file>$' (the path after the colon must equal the repo-relative path of the file itself, not any other path), classify the file as 'leaked'. If the last line does not match, classify the file as 'clean' and skip step 2 for that file. Capture the per-file classification in section 1 of the recovery report.\n\n2. Strip. For each leaked file, remove only that single trailing line and any trailing whitespace-only lines that were strictly between the body and the leaked marker. Do NOT modify any other line. Do NOT modify line endings of preserved lines. Do NOT add a trailing newline beyond what was present immediately before the leaked marker.\n\n3. JSON validation. For 091.json and 092.json, run json.load on the post-strip file. Both must parse cleanly. Capture the parsed top-level task_id in section 2 of the recovery report. If either file fails to parse, restore the file from git index and emit FAIL.\n\n4. Markdown validation. For 112.md, 113.md, 114.md, 115.md, assert the post-strip last non-empty line is NOT 'END_FILE' and is NOT prefixed by 'END_FILE'. Capture the post-strip last non-empty line of each file in section 3 of the recovery report.\n\n5. Materializer regex extension. Open claude_worklog/tools/claude_master_rebuild_planner.py. Locate function parse_begin_file_blocks (around line 291). Replace the strict regex from r'^BEGIN_FILE:?\\s*(.*?)\\n(.*?)\\nEND_FILE\\s*$' to r'^BEGIN_FILE:?\\s*(.*?)\\n(.*?)\\nEND_FILE(?::\\s*[^\\n]*)?\\s*$' so the closing marker may be either bare 'END_FILE' or 'END_FILE: <path>'. Additionally, in the fallback branch (around lines 305-306), replace the literal endswith('END_FILE') strip with a regex strip that also tolerates a trailing 'END_FILE: <anything>' line: use re.sub(r'\\nEND_FILE(?::[^\\n]*)?\\s*$', '', content). Do NOT change any other behavior of parse_begin_file_blocks. Do NOT change ALLOWED_MATERIALIZE_PREFIXES, FORBIDDEN_TEXT, sanitize_emitted_file_content, safe_materialize_blocks, or any other function.\n\n6. py_compile claude_worklog/tools/claude_master_rebuild_planner.py. Capture the exit code in section 4 of the recovery report. Non-zero exit is a hard fail; restore from git index and emit FAIL.\n\n7. Re-run a tiny sanity self-test by importing the module and calling parse_begin_file_blocks on three test inputs in-process: (a) a block ending with bare END_FILE; (b) a block ending with END_FILE: some/path; (c) a block ending with END_FILE: some/path followed by an extra blank line. All three must yield exactly one (rel_path, content) tuple where content does NOT contain the literal string 'END_FILE'. Capture the test outcomes in section 5 of the recovery report. Any failure is a hard fail.\n\n8. High-confidence secret scan. Run the canonical high-confidence secret scan over the eight modified files (six body-strip + materializer + recovery report). The scan MUST report zero matches. Capture the verbatim scan summary in section 6 of the recovery report. Any match is a hard fail; restore all modified files and emit FAIL.\n\n9. Cross-isolation git status. Run git status -s over v2/, /home/wali/Desktop/AI BOT, claude_worklog/autonomous_control_plane/, claude_worklog/requirements_inbox/, and claude_worklog/security/. Each path MUST report zero lines (the recovery is allowed to dirty only claude_worklog/agent_supervisor/tasks/, claude_worklog/phase2_core_rebuild/trainer_gpu_parity_impl/, claude_worklog/agent_supervisor_reliability/, and claude_worklog/tools/). Capture the verbatim output in section 7 of the recovery report. Any line outside the allowed scope is a hard fail.\n\n10. Emit claude_worklog/agent_supervisor_reliability/86_END_FILE_MARKER_LEAKAGE_RECOVERY_REPORT.md with sections: '1. Per-file classification', '2. JSON validation', '3. Markdown validation', '4. py_compile result', '5. parse_begin_file_blocks self-test', '6. Secret scan summary', '7. Cross-isolation git status', '8. Final per-file diff summary' (one bullet per modified file with line count delta), '9. Recommendation' (PASS or FAIL). Final marker line: PHASE2E1D_END_FILE_MARKER_LEAKAGE_RECOVERY_REPORT_READY. The report file MUST NOT contain any standalone trailing END_FILE marker line in its body.\n\n11. Emit claude_worklog/agent_supervisor_reliability/86_END_FILE_MARKER_LEAKAGE_RECOVERY_GO_NO_GO.md. Exactly one of:\n  PHASE2E1D_END_FILE_MARKER_LEAKAGE_RECOVERY_PASS\n  PHASE2E1D_END_FILE_MARKER_LEAKAGE_RECOVERY_FAIL\nNo other content. The marker file MUST NOT contain any trailing END_FILE marker line in its body.\n\nStop conditions: any modification to /home/wali/Desktop/AI BOT, any v2/ change, any prior-milestone trainer-parity file change, any Redis read or write, any live service restart, any exchange action, any leverage / margin change, any deploy intent, any secret-shaped string in the diff, any modification to a body line of 091, 092, 112, 113, 114, or 115 other than removal of the leaked trailing marker, or any modification to claude_master_rebuild_planner.py outside parse_begin_file_blocks is an unconditional FAIL with no autofix path; restore from git and surface to human attention.",
  "next_recommended_action": "On PHASE2E1D_END_FILE_MARKER_LEAKAGE_RECOVERY_PASS, supervisor unblocks dispatch of 091. On PHASE2E1D_END_FILE_MARKER_LEAKAGE_RECOVERY_FAIL with concrete blockers and no safety violation, supervisor dispatches a narrow REQ_0007 / REQ_0014 autofix task scoped to the same six files plus claude_worklog/tools/claude_master_rebuild_planner.py only. On any safety violation, surface to human attention; no autofix is permitted."
}
