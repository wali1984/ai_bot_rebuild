# Ollama Model Required

No local Ollama model is currently installed (`ollama list` returned empty).

Recommended next manual command:

```bash
ollama pull qwen2.5-coder:7b
```

Do not pull automatically unless user approval is provided.
