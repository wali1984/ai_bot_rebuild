# PLANNER TURN — Phase 2T Open Codex Review (Lane B `explainability_ui`; Codex review dispatched via Lane C `codex_watchdog`)

## Active requirement

REQ_0006 (Phase 2 trainer-parity rebuild) under concurrent enforcement of REQ_0009 (full decision explainability and under-the-hood UI), REQ_0017 (force paper/backtest MVP track), REQ_0018 (planner lane lock), REQ_0020 (full autonomous legacy-mapped paper/backtest performance target), REQ_0014 / REQ_0015 / REQ_0016 (Codex non-live human-replacement watchdog and planner-level human-attention autorecovery), REQ_0011 / REQ_0021 (parallel Codex review and capacity scheduler), REQ_0007 (Codex autofix authority for non-live blockers), REQ_0019 / REQ_0023 (legacy monitor / read-only audit evidence consulted during V2 build), REQ_0022 (LAB hedge-unwind / squeeze legacy failure mapped into the typed evidence pack as a deterministic per-step pointer literal).

## Active milestone

REQ_0017 milestone 8 `V2_BACKTEST_AND_PAPER_MVP_READY` closed with body line one at `claude_worklog/phase2_core_rebuild/v2_backtest_and_paper_mvp_ready/06_GO_NO_GO.md` and Codex PASS body line one `V2_BACKTEST_AND_PAPER_MVP_READY_CODEX_PASS` at `claude_worklog/phase2_core_rebuild/v2_backtest_and_paper_mvp_ready/10_GO_NO_GO_CODEX.md`. The five post-consolidation Lane A evidence-collection harnesses are all closed (Phase 2M `PHASE2M_REPLAY_CASE_LAB_HEDGE_UNWIND_CODEX_PASS`, Phase 2N `PHASE2N_PAPER_MODE_EVIDENCE_COLLECTION_HARNESS_CODEX_PASS`, Phase 2O `PHASE2O_SHADOW_MODE_EVIDENCE_COLLECTION_HARNESS_CODEX_PASS`, Phase 2P `PHASE2P_HISTORICAL_PNL_REPLAY_WIRING_CODEX_PASS`, Phase 2Q `PHASE2Q_AGGREGATE_EVIDENCE_ROLLUP_HARNESS_CODEX_PASS`). Phase 2R first post-consolidation Lane B explainability_ui milestone is closed at `decision_explainability_data_contract/09_CODEX_GO_NO_GO.md` body line one `PHASE2R_DECISION_EXPLAINABILITY_DATA_CONTRACT_CODEX_PASS`. Phase 2S second post-consolidation Lane B explainability_ui milestone is closed at `decision_explainability_paper_ledger_projection/09_CODEX_GO_NO_GO.md` body line one `PHASE2S_DECISION_EXPLAINABILITY_PAPER_LEDGER_PROJECTION_CODEX_PASS`. Phase 2T — replay-backtest-step / replay-backtest-summary explainability projection harness — is the **third post-consolidation Lane B `explainability_ui` milestone** per `claude_worklog/phase2_core_rebuild/v2_backtest_and_paper_mvp_ready/07_NEXT_STEP_AFTER_CONSOLIDATION.md` § "Lane B — explainability_ui (post-consolidation; backed only by real lineage)" and per the explicit `next_recommended_action` declared by `claude_worklog/agent_supervisor/tasks/176_phase2s_decision_explainability_paper_ledger_projection_codex_review.json`. This planner turn dispatches the Phase 2T Codex review.

## Trigger

Phase 2T implementation files are materialized under the closed-loop Codex recovery `178_codex_closed_loop_recover_177_decision_explainability_projection` (recovery report at `claude_worklog/phase2_core_rebuild/decision_explainability_replay_backtest_projection/178_CODEX_CLOSED_LOOP_RECOVERY_177_REPORT.md`; recovery GO/NO-GO body line one `CODEX_CLOSED_LOOP_RECOVERY_177_READY` at `claude_worklog/phase2_core_rebuild/decision_explainability_replay_backtest_projection/178_CODEX_CLOSED_LOOP_RECOVERY_177_GO_NO_GO.md`):

- `v2/backend/tests/unit/decision_explainability_replay_backtest_projection/__init__.py`
- `v2/backend/tests/unit/decision_explainability_replay_backtest_projection/fixtures.py`
- `v2/backend/tests/unit/decision_explainability_replay_backtest_projection/harness.py`
- `v2/backend/tests/unit/decision_explainability_replay_backtest_projection/test_decision_explainability_replay_backtest_projection.py`
- `claude_worklog/phase2_core_rebuild/decision_explainability_replay_backtest_projection/06_IMPLEMENTATION_REPORT.md` (body line last `PHASE2T_DECISION_EXPLAINABILITY_REPLAY_BACKTEST_PROJECTION_IMPLEMENTATION_REPORT_READY`)
- `claude_worklog/phase2_core_rebuild/decision_explainability_replay_backtest_projection/07_GO_NO_GO.md` (body line one `PHASE2T_DECISION_EXPLAINABILITY_REPLAY_BACKTEST_PROJECTION_IMPLEMENTATION_READY`)

The Phase 2T implementation packet was recovered by the closed-loop Codex recovery under task 178 after the original `177_phase2t_decision_explainability_replay_backtest_projection_implementation` Claude run exhausted retries without materializing files. Per REQ_0014 / REQ_0015 evidence-first reconciliation, the GO/NO-GO PASS marker at `07_GO_NO_GO.md` body line one overrides any stale queue `human_attention_required` status of the original 177 supervisor task; no further 177-implementation re-dispatch is required, and the predecessor surface area for the Phase 2T Codex review is the file marker, not the queue task status.

## Classification

Lane B `explainability_ui` data-contract sub-task. The Codex review itself runs under Lane C `codex_watchdog` per the standing parallel-capacity rule (Codex executes the review while the Claude planner remains free to author downstream Lane B UI panel planning artifacts). No new lineage IDs, no new typed surfaces, no new execution-side surfaces, no Redis access, no `/home/wali/Desktop/AI BOT` mutation, no live-readiness gate flip, no Binance read-only account-history endpoint invocation. The Phase 2T packet is test-only fixture / harness / pytest authoring driving the existing `ReplayBacktestRunner` composition root once at harness level via `build_replay_backtest_runner(now_ms_clock=build_replay_clock())` and the existing `PaperExecutionLedgerRecorder` composition root once at harness level via `build_paper_execution_ledger_recorder(now_ms_clock=build_paper_ledger_clock())`, invoking the recorder closure once per fixture row to obtain a typed `PaperExecutionLedgerEntry`, invoking `runner.assemble_step` once per fixture row to obtain a typed `ReplayBacktestStep`, and invoking `runner.assemble_summary` once per scenario to obtain a typed `ReplayBacktestSummary`, plus test-only typed value classes (`ReplayBacktestStepExplainabilityFixtureInput`, `ReplayBacktestStepExplainabilityEnvelope`, `ReplayBacktestSummaryExplainabilityEnvelope`, `ReplayBacktestProjectionHarnessResult`); the Codex review verifies that the fixtures, the pure-function harness module, the pytest module, the implementation report, and the GO/NO-GO marker all conform to the planning artifacts (01–05) and that no out-of-scope file under `v2/backend/app/` or `v2/frontend/` was modified.

## Decision

Author and queue Codex review task `179_phase2t_decision_explainability_replay_backtest_projection_codex_review` in this same planner turn. The task scope is:

- Spec / planning inputs (read-only): `01_LEGACY_FAILURE_EVIDENCE.md`, `02_TYPED_INPUT_FIXTURE_SPEC.md`, `03_HARNESS_PIPELINE_SPEC.md`, `04_TEST_PLAN.md`, `05_GO_NO_GO_REQUEST.md`, `PLANNER_TURN_2T_OPEN_IMPLEMENTATION.md`, this planner-turn note (`PLANNER_TURN_2T_OPEN_CODEX_REVIEW.md`).
- Implementation evidence (read-only): `06_IMPLEMENTATION_REPORT.md`, `07_GO_NO_GO.md` body line one (must equal `PHASE2T_DECISION_EXPLAINABILITY_REPLAY_BACKTEST_PROJECTION_IMPLEMENTATION_READY`), the closed-loop Codex recovery evidence at `178_CODEX_CLOSED_LOOP_RECOVERY_177_REPORT.md` and `178_CODEX_CLOSED_LOOP_RECOVERY_177_GO_NO_GO.md` (body line one `CODEX_CLOSED_LOOP_RECOVERY_177_READY`), and the four test-only files under `v2/backend/tests/unit/decision_explainability_replay_backtest_projection/`.
- Codex review output surface (write-only): `08_CODEX_REVIEW.md` (carrying `PHASE2T_DECISION_EXPLAINABILITY_REPLAY_BACKTEST_PROJECTION_CODEX_REVIEW_READY`) and `09_CODEX_GO_NO_GO.md` (one line: `PHASE2T_DECISION_EXPLAINABILITY_REPLAY_BACKTEST_PROJECTION_CODEX_PASS` or `PHASE2T_DECISION_EXPLAINABILITY_REPLAY_BACKTEST_PROJECTION_CODEX_FAIL`).
- Predecessor evidence: `PHASE2T_DECISION_EXPLAINABILITY_REPLAY_BACKTEST_PROJECTION_IMPLEMENTATION_READY` at `07_GO_NO_GO.md` body line one, `PHASE2S_DECISION_EXPLAINABILITY_PAPER_LEDGER_PROJECTION_CODEX_PASS` at `decision_explainability_paper_ledger_projection/09_CODEX_GO_NO_GO.md` body line one, `PHASE2R_DECISION_EXPLAINABILITY_DATA_CONTRACT_CODEX_PASS` at `decision_explainability_data_contract/09_CODEX_GO_NO_GO.md` body line one, `PHASE2Q_AGGREGATE_EVIDENCE_ROLLUP_HARNESS_CODEX_PASS` at `aggregate_evidence_rollup_harness/09_CODEX_GO_NO_GO.md` body line one, `PHASE2P_HISTORICAL_PNL_REPLAY_WIRING_CODEX_PASS` at `historical_pnl_replay_wiring/09_CODEX_GO_NO_GO.md` body line one, `PHASE2O_SHADOW_MODE_EVIDENCE_COLLECTION_HARNESS_CODEX_PASS` at `shadow_mode_evidence_collection_harness/09_CODEX_GO_NO_GO.md` body line one, `PHASE2N_PAPER_MODE_EVIDENCE_COLLECTION_HARNESS_CODEX_PASS` at `paper_mode_evidence_collection_harness/09_CODEX_GO_NO_GO.md` body line one, `PHASE2M_REPLAY_CASE_LAB_HEDGE_UNWIND_CODEX_PASS` at `replay_case_lab_hedge_unwind/09_CODEX_GO_NO_GO.md` body line one, `V2_BACKTEST_AND_PAPER_MVP_READY` at `v2_backtest_and_paper_mvp_ready/06_GO_NO_GO.md` body line one, and `V2_BACKTEST_AND_PAPER_MVP_READY_CODEX_PASS` at `v2_backtest_and_paper_mvp_ready/10_GO_NO_GO_CODEX.md` body line one.

Codex review must verify, at a minimum:

- Exactly four authored Python files under `v2/backend/tests/unit/decision_explainability_replay_backtest_projection/`: `__init__.py`, `fixtures.py`, `harness.py`, `test_decision_explainability_replay_backtest_projection.py`.
- The four deterministic scenarios per `02_TYPED_INPUT_FIXTURE_SPEC.md` are exercised by the fixture pack: `replay_step_explainability_pack_btc_winner_long` ×3 typed `ReplayBacktestStepExplainabilityFixtureInput` rows, `replay_step_explainability_pack_eth_winner_short` ×3, `replay_step_explainability_pack_lab_loser_short` ×3 carrying the LAB hedge-unwind / squeeze legacy-failure pointer literal `legacy_evidence__replay_step_explainability__lab_hedge_unwind_squeeze__step_<N>`, `replay_step_explainability_pack_sol_orchestrator_held` ×3; total 12 typed input rows organised into 4 typed `ReplayBacktestRun` scenarios.
- Per-row step envelope output: 12 typed `ReplayBacktestStepExplainabilityEnvelope` rows (one per typed input row) with exactly 17 fields (`replay_step_id`, `replay_run_id`, `paper_trade_id`, `risk_decision_id`, `decision_id`, `prediction_id`, `feature_snapshot_id`, `symbol`, `step_ts_ms`, `step_action`, `step_reason_code`, `input_paper_action`, `input_paper_reason_code`, `live_blocked`, `source_scenario_slug`, `step_index`, `legacy_evidence_pointer`); each envelope is `@dataclass(frozen=True)`.
- Per-scenario summary envelope output: 4 typed `ReplayBacktestSummaryExplainabilityEnvelope` rows (one per scenario) with exactly 14 fields (`replay_summary_id`, `replay_run_id`, `summary_emitted_ts_ms`, `total_steps_count`, `record_allow_steps_count`, `record_deny_steps_count`, `mirror_allow_proceed_long_steps_count`, `mirror_allow_proceed_short_steps_count`, `mirror_deny_orchestrator_held_steps_count`, `mirror_deny_orchestrator_abstained_steps_count`, `mirror_deny_default_steps_count`, `live_blocked`, `source_scenario_slug`, `legacy_evidence_pointer`); each envelope is `@dataclass(frozen=True)`.
- Harness-level invariants per `03_HARNESS_PIPELINE_SPEC.md`: the pure-function harness drives the existing `build_replay_backtest_runner(now_ms_clock=build_replay_clock())` factory **once at harness level** (one factory invocation, one runner closure produced) and the existing `build_paper_execution_ledger_recorder(now_ms_clock=build_paper_ledger_clock())` factory **once at harness level** (one factory invocation, one recorder closure produced); the recorder closure is invoked exactly 12 times (once per step row), the `runner.assemble_step` closure is invoked exactly 12 times (once per step row), and the `runner.assemble_summary` closure is invoked exactly 4 times (once per scenario); no per-row composition-root re-build; no `PaperModeRuntime` invocation; no `RiskDecisionEvaluator` invocation; no `OrchestratorDecisionRouter` invocation; no direct `assemble_paper_execution_ledger_entry`, `assemble_replay_backtest_step`, or `assemble_replay_backtest_summary` call from the harness module (the harness must invoke the closures produced by the factories); only deterministic in-memory projection of each per-row `ReplayBacktestStep` plus the fixture-row metadata into a `ReplayBacktestStepExplainabilityEnvelope` and each per-scenario `ReplayBacktestSummary` plus the scenario metadata into a `ReplayBacktestSummaryExplainabilityEnvelope`; the test-only `ReplayBacktestStepExplainabilityFixtureInput`, `ReplayBacktestStepExplainabilityEnvelope`, `ReplayBacktestSummaryExplainabilityEnvelope`, and `ReplayBacktestProjectionHarnessResult` value classes are `@dataclass(frozen=True)` under the unit-test package and are **not** V2 `app/domain` types, services, adapters, persistence models, API surfaces, schedulers, paper-mode trader processes, replay engines, or live-readiness gates; the harness preserves any underlying domain errors unchanged.
- The fixture-identity invariants per `02_TYPED_INPUT_FIXTURE_SPEC.md`: per-scenario slug namespacing across the four scenarios, distinct `risk_decision_id` / `decision_id` / `prediction_id` / `feature_snapshot_id` per row across the 12 rows, deterministic `legacy_evidence_pointer` strings of the form `legacy_evidence__replay_step_explainability__btc_winner_long__step_<N>`, `legacy_evidence__replay_step_explainability__eth_winner_short__step_<N>`, `legacy_evidence__replay_step_explainability__lab_hedge_unwind_squeeze__step_<N>` (LAB scenario), `legacy_evidence__replay_step_explainability__sol_orchestrator_held__step_<N>`, and the corresponding `..._summary` literals for the per-scenario summary envelopes, no wall-clock helper invocation, no file I/O, no network client import, no environment-variable reader, no heavyweight numerics import, no Redis adapter, all symbols uppercase Binance USD-M tradable symbols `BTCUSDT` / `ETHUSDT` / `LABUSDT` / `SOLUSDT`, every `RiskDecisionRecord.live_blocked` is `True`, every `step_envelope.live_blocked` is `True`, every `summary_envelope.live_blocked` is `True`.
- The 10 required pytest functions per `04_TEST_PLAN.md` § "Required pytest functions (10 total)" all pass under `.venv/bin/python -m pytest v2/backend/tests/unit/decision_explainability_replay_backtest_projection/test_decision_explainability_replay_backtest_projection.py -v --no-header`. The expected functions are: `test_harness_returns_frozen_result_with_12_steps_and_4_summaries`, `test_per_row_lineage_carry_over`, `test_per_row_action_reason_mirror_per_scenario`, `test_per_row_symbol_mirror_per_scenario`, `test_per_row_step_ts_ms_strictly_monotonic_and_within_replay_clock_window`, `test_summary_partition_counts_match_per_scenario_action_reason_distribution`, `test_lab_scenario_legacy_evidence_pointer_literal`, `test_envelope_allowed_fields_only`, `test_replay_clock_and_paper_ledger_clock_factory_determinism`, `test_forbidden_token_and_forbidden_import_scan`.
- Lineage carry-over coverage is asserted from the per-row `ReplayBacktestStep` (`replay_step_id`, `replay_run_id`, `paper_trade_id`, `risk_decision_id`, `decision_id`, `prediction_id`, `feature_snapshot_id`, `symbol`) into the per-row `ReplayBacktestStepExplainabilityEnvelope`; no lineage field is dropped or rewritten by the projection. The `paper_trade_id == 'pt_' + risk_decision_id` derivation invariant is preserved end-to-end and asserted by `test_per_row_lineage_carry_over`.
- Action / reason / step-timestamp / live-blocked mirroring coverage is asserted from the per-row `ReplayBacktestStep` (`step_action`, `step_reason_code`, `input_paper_action`, `input_paper_reason_code`, `step_ts_ms`, `live_blocked`) into the per-row `ReplayBacktestStepExplainabilityEnvelope`; no field is dropped or rewritten by the projection. Per-scenario action / reason-code expectations: BTC steps `step_action == "ALLOW"` / `step_reason_code == "PROCEED_LONG"`; ETH steps `step_action == "ALLOW"` / `step_reason_code == "PROCEED_SHORT"`; LAB steps `step_action == "ALLOW"` / `step_reason_code == "PROCEED_SHORT"`; SOL steps `step_action == "DENY"` / `step_reason_code == "ORCHESTRATOR_HELD"`. All BTC / ETH / LAB step input rows carry `input_paper_action == "ALLOW"` and `input_paper_reason_code == "RISK_OK"`; SOL step input rows carry `input_paper_action == "DENY"` and `input_paper_reason_code == "ORCHESTRATOR_HELD"`.
- Per-scenario summary partition counts: BTC summary `record_allow_steps_count == 3`, `mirror_allow_proceed_long_steps_count == 3`, all other partition counts == 0; ETH summary `record_allow_steps_count == 3`, `mirror_allow_proceed_short_steps_count == 3`, all other partition counts == 0; LAB summary `record_allow_steps_count == 3`, `mirror_allow_proceed_short_steps_count == 3`, all other partition counts == 0; SOL summary `record_deny_steps_count == 3`, `mirror_deny_orchestrator_held_steps_count == 3`, all other partition counts == 0; every summary `total_steps_count == 3`, `live_blocked is True`.
- Replay-clock and paper-ledger-clock factory determinism: `build_replay_clock()` and `build_paper_ledger_clock()` return deterministic incrementing test-clock closures; running `run_replay_backtest_projection_harness()` twice in succession returns result objects whose `step_envelopes` and `summary_envelopes` tuples are field-by-field equal across all 12+4 rows; factory closures isolate state between invocations (no global counter leakage), asserted by `test_replay_clock_and_paper_ledger_clock_factory_determinism`.
- The `replay_step_explainability_pack_lab_loser_short` scenario's per-step pointer literal matches `legacy_evidence__replay_step_explainability__lab_hedge_unwind_squeeze__step_<N>` for `N in {0, 1, 2}` and the harness never opens, dereferences, or reads the pointer string as a filesystem path; the LAB summary envelope carries `legacy_evidence_pointer == "legacy_evidence__replay_step_explainability__lab_hedge_unwind_squeeze__summary"`; the LAB-scenario `step_index` values are `{0, 1, 2}` (set equality); asserted by `test_lab_scenario_legacy_evidence_pointer_literal`.
- Forbidden-token / forbidden-import / forbidden-cross-test-import sweep per `test_forbidden_token_and_forbidden_import_scan`: the harness source contains exactly one occurrence of `build_paper_execution_ledger_recorder(` and exactly one occurrence of `build_replay_backtest_runner(`; the harness source imports from `v2.backend.app.composition.paper_execution_ledger.runtime` and from the existing replay-backtest-runner composition-root module; the harness source does **not** contain any of `build_paper_mode_runtime`, `assemble_paper_mode_flag`, `build_risk_decision_evaluator`, `assemble_risk_decision_record`, `build_orchestrator_decision_router`, `assemble_paper_execution_ledger_entry(`, `assemble_replay_backtest_step(`, `assemble_replay_backtest_summary(`, `open(`, `pathlib`, `sqlite3`, `redis`, `requests`, `httpx`, `socket`, `urllib`, `json.dump`, `json.load`, `pickle`, `csv.writer`, `csv.reader`; the combined source (harness + fixtures + tests) does **not** contain any of `time.time`, `time.monotonic`, `datetime.now`, `datetime.utcnow`, `os.environ`, `os.getenv`, `os.path`, `Path(`, `import socket`, `import requests`, `import httpx`, `import urllib`, `import redis`, `import aioredis`, `import ccxt`, `import fastapi`, `import starlette`, `import pydantic`, `import torch`, `import numpy`, `import pandas`, `import scikit-learn`, `import sklearn`, `mock(`, `Mock(`, `MagicMock(`, `patch(`, `monkeypatch`, `mocker`, `pnl`, `quantity`, `price`, `fee`, `funding`, `oi_`, `liquidation`, `orderbook`, `hedge_state`, `residual_exposure`, `squeeze`, `confidence`, `top_positive`, `top_negative`, `feature_freshness`, `regime_context`, `model_version`, `checkpoint_version`, `position_sizing`, `risk_check_list`, `blocked_trade_reason`, `paper_shadow_legacy_comparison`, `audit_timeline`, `shadow_decision_id`, `execution_intent_id`; the combined source does **not** contain any `import` line naming any test module from `v2/backend/tests/unit/decision_explainability_data_contract/`, `v2/backend/tests/unit/decision_explainability_paper_ledger_projection/`, `v2/backend/tests/unit/paper_mode_evidence_collection_harness/`, `v2/backend/tests/unit/shadow_mode_evidence_collection_harness/`, `v2/backend/tests/unit/historical_pnl_replay_wiring/`, or `v2/backend/tests/unit/aggregate_evidence_rollup_harness/`; no authored line under any of the three Phase 2T source-bearing files equals the literal harness framing-token markers (the safety check is enforced via runtime string concatenation in the test so the test source itself does not contain the bare token literals).
- No file under `v2/backend/app/` is modified by the Phase 2T packet.
- No file under `v2/frontend/` is modified by the Phase 2T packet.
- No file under `/home/wali/Desktop/AI BOT` is modified.
- No Redis access of any kind.
- No live service restart, no exchange action, no leverage / margin change, no live-trading enablement, no deployment, no production migration, no secret exposure.
- No Binance read-only account-history endpoint invocation, no Binance HTTP API invocation of any kind by the Phase 2T packet.
- No new lineage ID is introduced beyond `replay_step_id`, `replay_run_id`, `replay_summary_id`, `paper_trade_id`, `risk_decision_id`, `decision_id`, `prediction_id`, and `feature_snapshot_id` (all of which are already-typed surface fields on `ReplayBacktestStep` / `ReplayBacktestSummary` / `PaperExecutionLedgerEntry`).
- No `shadow_decision_id` or `execution_intent_id` lineage row is introduced.
- No `top_positive_feature_contributors`, `top_negative_feature_contributors`, `feature_freshness_flags`, `stale_missing_unused_feature_flags`, `confidence`, `previous_confidence`, `confidence_delta`, `confidence_calibration`, `model_version`, `checkpoint_version`, `regime_context`, `position_sizing_reason`, `risk_check_list`, `blocked_trade_reason`, `paper_shadow_legacy_comparison`, or `audit_timeline` field is introduced on the step or summary explainability envelopes (those belong to separate, later milestones explicitly out of scope at Phase 2T; their underlying lineage / projection subsystems do not exist at consolidation HEAD).
- No PnL, position sizing, quantity, price, fees, slippage, funding, OI, liquidation map, orderbook depth, hedge-state, residual-exposure, or squeeze-risk computation is introduced.
- No ledger persistence is introduced (no SQL, no SQLite, no JSON file, no Parquet, no CSV, no Redis, no in-memory dict acting as a ledger).
- The live-readiness gate is not flipped; `FINAL_NON_LIVE_REBUILD_READY_FOR_LIVE_GATE_REVIEW` remains a separate downstream artifact requiring explicit human approval.
- No placeholder file `v2/backend/app/services/paper_loop.py` or `v2/backend/app/services/replay_runner.py` is modified.
- No populating of `v2/backend/app/domain/replay/` or `v2/backend/app/domain/execution/` occurs at the Phase 2T layer.
- No `v2/backend/app/composition/v2_backtest_and_paper_mvp_ready.py` flat-file placeholder is introduced.
- No `mock`, `patch`, or `monkeypatch` is applied to `build_paper_execution_ledger_recorder`, `assemble_paper_execution_ledger_entry`, `build_replay_backtest_runner`, `assemble_replay_backtest_step`, `assemble_replay_backtest_summary`, `build_paper_mode_runtime`, `assemble_paper_mode_flag`, `build_risk_decision_evaluator`, `assemble_risk_decision_record`, or `build_orchestrator_decision_router`.
- The Phase 2T test or harness modules do not import any test module from `v2/backend/tests/unit/decision_explainability_data_contract/`, `v2/backend/tests/unit/decision_explainability_paper_ledger_projection/`, `v2/backend/tests/unit/paper_mode_evidence_collection_harness/`, `v2/backend/tests/unit/shadow_mode_evidence_collection_harness/`, `v2/backend/tests/unit/historical_pnl_replay_wiring/`, or `v2/backend/tests/unit/aggregate_evidence_rollup_harness/` (Phase 2T mirrors the typed `RiskDecisionRecord` / `PaperExecutionLedgerEntry` carriers of those harnesses through fresh in-package fixtures, not by cross-importing prior test packages).

## Codex review marker

- PASS body line one: `PHASE2T_DECISION_EXPLAINABILITY_REPLAY_BACKTEST_PROJECTION_CODEX_PASS`.
- FAIL body line one: `PHASE2T_DECISION_EXPLAINABILITY_REPLAY_BACKTEST_PROJECTION_CODEX_FAIL`.
- On Codex FAIL with concrete documentation blockers and no safety violation, supervisor dispatches a REQ_0007 / REQ_0014 autofix task scoped to the Phase 2T packet only.
- If the FAIL is a stale-rubric / pre-existing-placeholder false positive analogous to the 2H.A / 2H.B / 2H.C / 2I.A / 2I.B / 2I.C / 2J.C / 2L / 2M / 2N / 2O / 2P / 2Q / 2R / 2S reconciliation precedent, supervisor authors `10_CODEX_RECONCILIATION_ADDENDUM.md` under `claude_worklog/phase2_core_rebuild/decision_explainability_replay_backtest_projection/` and rewrites `09_CODEX_GO_NO_GO.md` body to PASS per the established reconciliation precedent.
- On any safety violation, surface to human attention; no autofix is permitted.

## Hard safety boundaries (restated)

- No `/home/wali/Desktop/AI BOT` mutation.
- No Redis read or write; no Redis adapter.
- No live service restart; no live trading enablement.
- No exchange action; no leverage / margin change.
- No deployment; no production migration.
- No secret exposure or commit.
- No flip of `FINAL_NON_LIVE_REBUILD_READY_FOR_LIVE_GATE_REVIEW`.
- No live Binance API call; no Binance HTTP API of any kind.
- No file under `v2/backend/app/` modified at the Phase 2T layer.
- No file under `v2/frontend/` modified at the Phase 2T layer.
- No file under any prior-milestone Phase 2 directory modified at the Phase 2T layer.
- No standalone harness framing-token marker line in any authored file body.

## Authored task

This planner turn authors:

- This planner-turn note (`PLANNER_TURN_2T_OPEN_CODEX_REVIEW.md`) under `claude_worklog/phase2_core_rebuild/decision_explainability_replay_backtest_projection/`.
- The supervisor task `179_phase2t_decision_explainability_replay_backtest_projection_codex_review` under `claude_worklog/agent_supervisor/tasks/`.
- The queued `codex_recover_179_phase2t_decision_explainability_replay_backtest_projection_codex_review` shadow recovery task under `claude_worklog/agent_supervisor/tasks/` per the standing precedent (REQ_0007 / REQ_0011 / REQ_0014 / REQ_0016 / REQ_0021).

## Recovery posture (Codex autofix lane)

Per REQ_0007 / REQ_0011 / REQ_0014 / REQ_0016 / REQ_0021, on a Codex FAIL with concrete documentation blockers and no safety violation, the supervisor dispatches a Codex autofix scoped to the Phase 2T review packet only (`08_CODEX_REVIEW.md` and `09_CODEX_GO_NO_GO.md`). If the FAIL is a stale-rubric / pre-existing-placeholder false positive, the supervisor authors `10_CODEX_RECONCILIATION_ADDENDUM.md` under `claude_worklog/phase2_core_rebuild/decision_explainability_replay_backtest_projection/` and rewrites the `09_CODEX_GO_NO_GO.md` body to PASS per the established reconciliation precedent. On any safety violation, surface to human attention; no autofix is permitted.

PHASE2T_DECISION_EXPLAINABILITY_REPLAY_BACKTEST_PROJECTION_PLANNER_TURN_OPEN_CODEX_REVIEW_READY
