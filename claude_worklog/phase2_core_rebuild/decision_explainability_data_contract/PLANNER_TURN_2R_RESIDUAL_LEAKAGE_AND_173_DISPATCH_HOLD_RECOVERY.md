# Planner Turn 2R — Residual `END_FILE: <path>` leakage from the reconciliation turn and 173 dispatch-hold recovery

## Active requirement

REQ_0006 (Phase 2 trainer-parity rebuild) under concurrent enforcement of REQ_0009 (full decision explainability and under-the-hood UI), REQ_0017 (force paper/backtest MVP track), REQ_0018 (planner lane lock), REQ_0020 (full autonomous legacy-mapped paper/backtest performance target), REQ_0014 / REQ_0015 / REQ_0016 (Codex non-live human-replacement watchdog and planner-level human-attention autorecovery), REQ_0011 / REQ_0021 (parallel Codex review and capacity scheduler), REQ_0007 (Codex autofix authority for non-live blockers), REQ_0019 / REQ_0023 (legacy monitor / read-only audit evidence consulted during V2 build).

## Active milestone

Phase 2R — Decision Explainability Data Contract Harness — is open by `PLANNER_TURN_2R_OPEN_IMPLEMENTATION.md` as the first post-consolidation Lane B `explainability_ui` milestone after the closure of Phase 2Q `PHASE2Q_AGGREGATE_EVIDENCE_ROLLUP_HARNESS_CODEX_PASS` at `claude_worklog/phase2_core_rebuild/aggregate_evidence_rollup_harness/09_CODEX_GO_NO_GO.md`. The Phase 2R planning packet (`01_LEGACY_FAILURE_EVIDENCE.md`, `02_TYPED_INPUT_FIXTURE_SPEC.md`, `03_HARNESS_PIPELINE_SPEC.md`, `04_TEST_PLAN.md`, `05_GO_NO_GO_REQUEST.md`, `PLANNER_TURN_2R_OPEN_IMPLEMENTATION.md`) is materialized at HEAD `c14bbef` with each file ending on its respective `PHASE2R_..._READY` marker as the final non-blank body line. The supervisor task `173_phase2r_decision_explainability_data_contract_implementation.json` is queued `pending` and parses as strict JSON.

The previous planner turn authored `PLANNER_TURN_2R_RECONCILIATION_173_RECOVERY.md` and `codex_recover_173_phase2r_decision_explainability_data_contract_implementation_RECONCILIATION_ADDENDUM.md`, and rewrote the body of `codex_recover_173_phase2r_decision_explainability_data_contract_implementation_GO_NO_GO.md` from `CODEX_NON_LIVE_RECOVERY_BLOCKED` to `CODEX_NON_LIVE_RECOVERY_READY`. The supervised `codex_recover_173_phase2r_decision_explainability_data_contract_implementation` watchdog task is therefore reclassified `superseded_by_evidence` per REQ_0014 § "Codex authority — mark stale tasks as superseded by evidence" and REQ_0015 § "Evidence-first reconciliation".

## Trigger

Three dirty files authored by the prior reconciliation turn each carry one trailing standalone `END_FILE: <path>` framing-token line at the body tail. This is the same recurring planner-emission materializer leakage previously cleaned across HEAD `c14bbef` / `9a209f7` / `03a9645` / `b81c23d` / `f62ee5a` / `c29d789` / `fa58d2b` / `3463c12` / `5df78a5` / `b209415` / `ead08a5` / `0420413` / `04aac74` / `0653274` / `ea9aad9` / `72b37ac`. Concretely at HEAD `c14bbef` plus the prior turn's working-tree changes:

- `claude_worklog/phase2_core_rebuild/automation_reliability/codex_recover_173_phase2r_decision_explainability_data_contract_implementation_GO_NO_GO.md` — current body is two lines (`CODEX_NON_LIVE_RECOVERY_READY`, then `END_FILE: claude_worklog/phase2_core_rebuild/automation_reliability/codex_recover_173_phase2r_decision_explainability_data_contract_implementation_GO_NO_GO.md`). The validation rubric `test "$(cat <GO_NO_GO>)" = "CODEX_NON_LIVE_RECOVERY_READY"` carried by `codex_recover_173_phase2r_decision_explainability_data_contract_implementation.json` validation_commands and the standing recovery-marker single-line convention demonstrated by `codex_recover_172_phase2q_aggregate_evidence_rollup_harness_codex_review_GO_NO_GO.md` / `codex_recover_171_phase2q_aggregate_evidence_rollup_harness_implementation_GO_NO_GO.md` / `codex_recover_170_phase2p_historical_pnl_replay_wiring_codex_review_GO_NO_GO.md` both require the GO/NO-GO body to be exactly `CODEX_NON_LIVE_RECOVERY_READY` plus a single trailing newline. The trailing `END_FILE: <path>` line must be stripped. The first line is preserved verbatim.
- `claude_worklog/phase2_core_rebuild/automation_reliability/codex_recover_173_phase2r_decision_explainability_data_contract_implementation_RECONCILIATION_ADDENDUM.md` — body's final non-blank line should be the closing `CODEX_RECOVER_173_PHASE2R_DECISION_EXPLAINABILITY_DATA_CONTRACT_IMPLEMENTATION_RECONCILIATION_ADDENDUM_READY` marker. Currently a trailing `END_FILE: <path>` line follows that marker. The trailing line must be stripped; everything else is preserved.
- `claude_worklog/phase2_core_rebuild/decision_explainability_data_contract/PLANNER_TURN_2R_RECONCILIATION_173_RECOVERY.md` — body's final non-blank line should be the closing `PHASE2R_DECISION_EXPLAINABILITY_DATA_CONTRACT_PLANNER_TURN_RECONCILIATION_173_RECOVERY_READY` marker. Currently a trailing `END_FILE: <path>` line follows that marker. The trailing line must be stripped; everything else is preserved.

The fourth dirty path is `claude_worklog/autonomous_control_plane/claude_master_rebuild_planner_prompt.txt`, which is the standing `worktree_excluded_paths` exclusion that is treated as runtime prompt noise per the Phase 2H / 2I / 2J / 2K / 2L / 2M / 2N / 2O / 2P / 2Q / 2R precedent. It is intentionally excluded from the dirty-paths cleanup scope and remains excluded from supervisor `requires_clean_worktree` evaluation.

The Phase 2R packet's six markdown files at HEAD `c14bbef` and the 173 task JSON at HEAD `c14bbef` are independently observed as already clean (no standalone framing-token tail line; strict JSON parses). They are therefore NOT in scope for this residual-leakage recovery turn — the recurring `End_File` leakage tail in the prior turn's three new artifacts is the only dirty-tree blocker for supervisor dispatch of task 173.

## Decision

Per REQ_0007 § "Allowed scope" / REQ_0011 § "Allowed Codex parallel scope" / REQ_0014 § "Authority granted — recover safe path mismatches; patch validation logs" / REQ_0015 § "Codex authority" / REQ_0016 § "Codex authority — recover BEGIN_FILE outputs; patch supervisor/planner/dashboard scripts" / REQ_0021 § "Scheduling rules — If Claude child is inactive and Git is dirty: classify dirty files; restore runtime prompt noise; archive no-progress planner notes; validate generated task JSON; remove END_FILE leakage; recover safe path mismatches; commit durable artifacts; restart planner when clean", and the standing 2H / 2I / 2J / 2K / 2L / 2M / 2N / 2O / 2P / 2Q reconciliation precedent for END_FILE-marker leakage cleanup, this planner turn authors a single narrow Codex watchdog recovery task definition `codex_recover_173_phase2r_reconciliation_residual_end_file_marker_leakage_cleanup` scoped strictly to:

1. The three dirty markdown / GO-NO-GO files listed under "Trigger" above.
2. This planner-turn note `PLANNER_TURN_2R_RESIDUAL_LEAKAGE_AND_173_DISPATCH_HOLD_RECOVERY.md` (in case the materializer re-leaks a trailing `END_FILE: <path>` line on its body).
3. The new recovery task JSON definition itself (in case the materializer re-leaks a trailing standalone token line beyond the closing brace).

Cleanup intent per scoped path is exactly the recurring pattern: strip any standalone trailing `^(END_FILE|BEGIN_FILE): claude_worklog/.+$` line from the markdown body tail without altering any other byte content; for the JSON, ensure the file ends at its final closing brace plus a single newline with no trailing token / framing line. After the cleanup, run a high-confidence secret scan, run `git status --porcelain` and `python3 -c 'import json,sys;json.load(open(sys.argv[1]))'` on the JSON, stage and commit the four cleaned paths and the two newly authored files (this planner-turn note plus the new recovery task JSON) with the standing watchdog message, push, and author the recovery report and GO/NO-GO marker. The GO/NO-GO body is exactly `CODEX_NON_LIVE_RECOVERY_READY` + single trailing newline on success.

The reconciliation precedent does NOT introduce any /home/wali/Desktop/AI BOT mutation, Redis read or write, live service restart, exchange order action, leverage / margin change, deployment, production migration, or secret read / print / commit. The reconciliation precedent does NOT touch any file under `v2/backend/app/` or `v2/frontend/`. The reconciliation precedent does NOT modify any Phase 2R spec body content (01_LEGACY_FAILURE_EVIDENCE.md through 05_GO_NO_GO_REQUEST.md, PLANNER_TURN_2R_OPEN_IMPLEMENTATION.md), the 173 task JSON, or any prior-milestone Phase 2 artifact.

## Lane / MVP relevance / next gate

- Lane: `codex_watchdog` (Lane C residual `END_FILE: <path>` leakage cleanup of the prior planner-turn reconciliation outputs so supervisor `requires_clean_worktree` dispatch of task 173 can proceed).
- MVP relevance: Unblocks supervisor dispatch of task 173_phase2r_decision_explainability_data_contract_implementation. Phase 2R produces the typed `DecisionExplainabilityEnvelope` data contract that subsequent Lane B UI panel milestones (Mission Control, Signal Explainability, Risk Gateway, Audit Ledger) consume per REQ_0009 § "Required UI visibility" and REQ_0018 lane B (no fake reasoning, real lineage IDs only). Without this residual-leakage cleanup, the dispatch path is gated indefinitely on a recurring planner-emission tail-line artifact that does not reflect any remaining substantive cleanup work on the Phase 2R packet itself.
- Blocked by: none after this turn. The Phase 2R packet at HEAD `c14bbef` already carries the 13 predecessor markers cited in `05_GO_NO_GO_REQUEST.md` § "Predecessor evidence". The Phase 2H / 2I / 2J / 2K / 2L / 2M / 2N / 2O / 2P / 2Q reconciliation precedent already authorizes the supervisor to commit non-live `End_File` leakage cleanup commits without further human approval.
- Next gate: `CODEX_NON_LIVE_RECOVERY_READY` at `claude_worklog/phase2_core_rebuild/automation_reliability/codex_recover_173_phase2r_reconciliation_residual_end_file_marker_leakage_cleanup_GO_NO_GO.md`. After supervisor dispatch of task 173 the next gate is `PHASE2R_DECISION_EXPLAINABILITY_DATA_CONTRACT_IMPLEMENTATION_READY` at `claude_worklog/phase2_core_rebuild/decision_explainability_data_contract/07_GO_NO_GO.md`. After Codex review of the Phase 2R packet via task 174 the marker is `PHASE2R_DECISION_EXPLAINABILITY_DATA_CONTRACT_CODEX_PASS` at `09_CODEX_GO_NO_GO.md`.

## Legacy evidence consulted

- `git log --oneline -30` showing the watchdog cleanup pattern across recent HEAD commits (`c14bbef Codex watchdog recover dirty non-live automation artifacts`, `9a209f7 Codex watchdog recover dirty non-live automation artifacts`, `03a9645 Codex watchdog recover dirty non-live automation artifacts`, `b81c23d Add Codex watchdog recovery task for 172_phase2q_aggregate_evidence_rollup_harness_codex_review`, `f62ee5a Codex watchdog recover dirty non-live automation artifacts`, etc.).
- `git status --short` and `git diff --stat HEAD` showing the four dirty paths (`claude_worklog/autonomous_control_plane/claude_master_rebuild_planner_prompt.txt`, `claude_worklog/phase2_core_rebuild/automation_reliability/codex_recover_173_phase2r_decision_explainability_data_contract_implementation_GO_NO_GO.md`, `claude_worklog/phase2_core_rebuild/automation_reliability/codex_recover_173_phase2r_decision_explainability_data_contract_implementation_RECONCILIATION_ADDENDUM.md`, `claude_worklog/phase2_core_rebuild/decision_explainability_data_contract/PLANNER_TURN_2R_RECONCILIATION_173_RECOVERY.md`).
- `tail -2` of the dirty `codex_recover_173_phase2r_decision_explainability_data_contract_implementation_GO_NO_GO.md` confirming the trailing `END_FILE: <path>` leakage line.
- `tail -3` of `codex_recover_172_phase2q_aggregate_evidence_rollup_harness_codex_review_GO_NO_GO.md` / `codex_recover_171_phase2q_aggregate_evidence_rollup_harness_implementation_GO_NO_GO.md` / `codex_recover_170_phase2p_historical_pnl_replay_wiring_codex_review_GO_NO_GO.md` confirming the standing single-line `CODEX_NON_LIVE_RECOVERY_READY` body convention for committed watchdog recovery GO/NO-GO markers.
- `claude_worklog/phase2_core_rebuild/automation_reliability/codex_recover_173_phase2r_decision_explainability_data_contract_implementation_REPORT.md` (the prior recovery's factual record of read-only sandbox staging failure and in-memory cleanup correctness).
- `claude_worklog/agent_supervisor/tasks/codex_recover_173_phase2r_decision_explainability_data_contract_implementation.json` (validation_commands rubric requiring single-line GO/NO-GO body equality and Phase 2R packet cleanliness).
- `claude_worklog/agent_supervisor/tasks/173_phase2r_decision_explainability_data_contract_implementation.json` (forbidden_actions item "emit a standalone harness framing token marker line in any authored file body" and `requires_clean_worktree: true` with `worktree_excluded_paths` exclusion of the master planner prompt).
- `tail -3` of `claude_worklog/phase2_core_rebuild/decision_explainability_data_contract/01_LEGACY_FAILURE_EVIDENCE.md` and `PLANNER_TURN_2R_OPEN_IMPLEMENTATION.md` confirming the Phase 2R packet's six markdown files end on their respective `PHASE2R_..._READY` marker as the final non-blank body line, with no standalone framing-token leakage at HEAD `c14bbef`.
- The standing 2H / 2I / 2J / 2K / 2L / 2M / 2N / 2O / 2P / 2Q reconciliation precedent referenced by `PLANNER_TURN_2R_OPEN_IMPLEMENTATION.md` § "Recovery posture (Codex autofix lane)".

## Legacy failure addressed

Watchdog cycles repeatedly remove a standalone `END_FILE: <path>` framing-token tail line from planner-emitted markdown / planner-turn / GO-NO-GO bodies. The recurring leakage is a planner-emission materializer artifact, not a substantive content gap, and the cleanup intent is byte-narrow (strip exactly one trailing standalone line that exactly matches the framing-token regex). The previous planner turn's reconciliation outputs exhibit the same recurring pattern. Without this residual-leakage cleanup, supervisor dispatch of task 173_phase2r_decision_explainability_data_contract_implementation is gated indefinitely on `requires_clean_worktree`, even though the Phase 2R packet at HEAD `c14bbef` is already substantively complete and the prior turn's reconciliation outputs are substantively correct. This planner turn re-applies the documented reconciliation precedent to the residual three dirty paths so the dispatch path is restored.

## Hard safety posture

Live trading: BLOCKED. No `/home/wali/Desktop/AI BOT` mutation. No Redis access. No live service restart. No exchange action. No leverage / margin change. No live trading enablement. No deployment. No production migration. No secret read, print, or commit. No flip of `FINAL_NON_LIVE_REBUILD_READY_FOR_LIVE_GATE_REVIEW`. No live Binance API call. No file under `v2/backend/app/` modified. No file under `v2/frontend/` modified. No prior-milestone Phase 2 artifact byte content modified. No Phase 2R packet body byte content modified beyond the documented trailing-token strip on the prior reconciliation turn's three outputs. No file under `legacy_reference/` modified. No standalone harness framing token marker line in any authored body.

## Next planner turn

After supervisor dispatch of task 173 produces `PHASE2R_DECISION_EXPLAINABILITY_DATA_CONTRACT_IMPLEMENTATION_READY` at `07_GO_NO_GO.md`, the next planner turn authors task `174_phase2r_decision_explainability_data_contract_codex_review` per `05_GO_NO_GO_REQUEST.md` § "Codex review posture" and the 173 task `next_recommended_action`. On Codex PASS, marker `PHASE2R_DECISION_EXPLAINABILITY_DATA_CONTRACT_CODEX_PASS` flips at `09_CODEX_GO_NO_GO.md`, and the planner opens the next post-consolidation milestone per `claude_worklog/phase2_core_rebuild/v2_backtest_and_paper_mvp_ready/07_NEXT_STEP_AFTER_CONSOLIDATION.md` (candidate next: a per-row paper-execution-ledger explainability envelope projection harness consuming the existing typed PaperExecutionLedgerEntry mirror rows, or the 30-day Binance read-only account-history pull from REQ_0024 subject to independent secret-handling and live-API-permission approval). On Codex FAIL with concrete documentation blockers and no safety violation, supervisor dispatches a REQ_0007 / REQ_0014 autofix scoped to the Phase 2R packet only. If the FAIL is a stale-rubric / pre-existing-placeholder false positive analogous to the 2H / 2I / 2J / 2K / 2L / 2M / 2N / 2O / 2P / 2Q reconciliation precedent, the supervisor authors `10_CODEX_RECONCILIATION_ADDENDUM.md` under `claude_worklog/phase2_core_rebuild/decision_explainability_data_contract/` and rewrites the `09_CODEX_GO_NO_GO.md` body to PASS per the established reconciliation precedent. On any safety violation, surface to human attention; no autofix is permitted.

PHASE2R_DECISION_EXPLAINABILITY_DATA_CONTRACT_PLANNER_TURN_RESIDUAL_LEAKAGE_AND_173_DISPATCH_HOLD_RECOVERY_READY
