# Phase 2C Feature Snapshot Scope

Phase 2C creates the local, non-live feature snapshot and attribution foundation required before trainer parity work.

Scope:
- Preserve legacy `feature_pipeline.py` behavior.
- Do not modify or run legacy feature pipeline.
- Wrap legacy-style payloads into trainer-ready feature snapshots.
- Add lineage, freshness, stale/missing/unused flags, and source attribution metadata.

PHASE2_FEATURE_SNAPSHOT_SCOPE_READY
