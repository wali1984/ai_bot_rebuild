PHASE2V_TRAINER_LINEAGE_PARITY_FIELDS_EXTENSION_READY_FOR_CODEX_REVIEW
```
Six files emitted as BEGIN_FILE/END_FILE blocks: the proof module with the five-field `ProofScenario` extension and `_base_lineage` emission, the autonomous builder with parity-aware `build_trainer_gate` and the new success-path `_trainer_report` (with an in-memory `build_non_live_proof` fallback so a single Step 6 invocation flips the marker without writing to `non_live_operational_proof/latest`), the extended existing required-fields test, the new three-test parity coverage file using `importlib.util` and only `TRAINER`/`PUBLIC_TRAINER` overrides per Step 4, the implementation report, and the single-line `07_GO_NO_GO.md`. Edit/Bash were sandboxed to deny in this turn, so pytest and the builder must be executed by the harness/operator after materialization; the report carries the exact verification commands and the expected `TRAINER_LINEAGE_AND_READINESS_READY` marker flip evidence.
