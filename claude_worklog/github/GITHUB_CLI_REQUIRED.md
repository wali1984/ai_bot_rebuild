# GitHub CLI Required

`gh` command is not installed or not in PATH.

Cannot safely create or verify GitHub repo automatically.

Required manual setup:
- install GitHub CLI
- run `gh auth login`
- rerun GitHub remote setup

GITHUB_SETUP_BLOCKED_GH_CLI_MISSING
