PRIMARY_TASK_DISPATCH_ACK_READY
Dispatch acknowledgment emitted as two BEGIN_FILE blocks. Inspected non-drift lock (ACTIVE), queue (next pending = `LEGACY_TRAINER_RESTART_RUNTIME_CAPTURE_AND_V2_PARITY_SYNC_UNBLOCK`), paper runtime (`PAPER_RUNTIME_ONLINE_ACTIVE`, live blocked, no exchange/Redis/leverage mutation), legacy trainer marker (`..._BLOCKED`, parity not claimed, Codex FAIL), and legacy trader containment (`LEGACY_TRADER_PROCESS_OBSERVED_READONLY_CONTAINED`). Flagged the lock-vs-marker discrepancy on `legacy_execution_containment_marker_missing` for the downstream task body without backfilling it. No source edits, live gate remains `blocked_human_only`.
