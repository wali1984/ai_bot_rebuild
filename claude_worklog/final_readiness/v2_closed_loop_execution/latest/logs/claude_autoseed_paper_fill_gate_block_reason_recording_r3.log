I'm blocked by the harness's interactive Write/Edit permission prompts — every attempt to modify `v2/backend/app/services/edge_proof/replay_miner.py` or to drop the per-task report under `claude_worklog/final_readiness/v2_autonomous_mission_backlog/per_task/paper_fill_gate_block_reason_recording/IMPLEMENTATION_REPORT.md` returns `not granted yet`. Without permission to write anything to disk I can't materialise either the code change or the required report.

Here is the narrow analysis + the exact diff to apply once Edit/Write are granted (no live trading touched, no prediction made tradeable, no fabricated reasons).

**Gap (already-implemented surface ✓; missing surface ✗)**

- ✓ `v2/backend/app/services/edge_proof/replay_miner.py` resolves `paper_fill_gate_block_reasons` from `row`, `raw_row`, `paper_gate_decision`, `trainer_output`, and the boolean gate flags `pre_trade_allowed=False`, `fee_gate_allowed=False`, `churn_blocked=True`.
- ✓ `MISSING_SOURCE` lineage already kicks in when `paper_fill_allowed is False` (or `paper_intent.decision in {HELD_BY_PAPER_FILL_GATE, BLOCKED}`) and zero reasons were found, with `missing_reason="paper_fill_gate_block_reason_missing_from_v2_sources"`.
- ✓ `validate_bundle_row()` already enforces the contract; `backfill_bundle_replay_context()` already re-runs the resolver across the three persisted JSONL stores.
- ✓ `v2/backend/app/services/war_room/parallel_recovery_24h.py:341-365` already treats `MISSING_SOURCE` lineage as honest and only flags `paper_fill_gate_block_unrecorded_reason` when the lineage state is something else.
- ✗ The resolver never looks at `risk_decision.*`, even though the upstream V2 risk gateway publishes `pre_trade_allowed`, `churn_reason`, `fee_gate_reason`, `block_reasons`, and `paper_fill_gate_block_reasons` there. Bundles that *do* have evidence one hop away currently fall back to `MISSING_SOURCE` instead of recording the real reason.

**Proposed surgical change to `v2/backend/app/services/edge_proof/replay_miner.py`** (additive, lineage-only — does not change `paper_fill_allowed`, `label`, or `outcome_after_cost`):

```
- PAPER_FILL_GATE_EVIDENCE_SOURCES: tuple[str, ...] = (
-     "paper_fill_gate_block_reasons",
-     "paper_gate_decision.paper_fill_gate_block_reasons",
-     "trainer_output.paper_fill_gate_block_reasons",
-     "pre_trade_allowed",
-     "fee_gate_allowed",
-     "churn_blocked",
- )
+ PAPER_FILL_GATE_MISSING_SOURCE_REASON = (
+     "paper_fill_gate_block_reason_missing_from_v2_sources"
+ )
+ PAPER_FILL_GATE_EVIDENCE_SOURCES: tuple[str, ...] = (
+     "paper_fill_gate_block_reasons",
+     "paper_gate_decision.paper_fill_gate_block_reasons",
+     "trainer_output.paper_fill_gate_block_reasons",
+     "pre_trade_allowed",
+     "fee_gate_allowed",
+     "churn_blocked",
+     "risk_decision.paper_fill_gate_block_reasons",
+     "risk_decision.block_reasons",
+     "risk_decision.pre_trade_allowed",
+     "risk_decision.churn_reason",
+     "risk_decision.fee_gate_reason",
+ )
```

Inside `_resolve_paper_fill_gate_block_reasons(row)` (after the existing `_add_reasons(...)` calls and before `unique_reasons = _dedupe_strings(reasons)`):

```
risk_decision = row.get("risk_decision") if isinstance(
    row.get("risk_decision"), Mapping
) else {}
_add_reasons(
    "risk_decision.paper_fill_gate_block_reasons",
    risk_decision.get("paper_fill_gate_block_reasons"),
)
_add_reasons(
    "risk_decision.block_reasons",
    risk_decision.get("block_reasons"),
)
if risk_decision.get("pre_trade_allowed") is False:
    reasons.append("PRE_TRADE_GATE_BLOCKED")
    evidence_sources.append({
        "field": "risk_decision.pre_trade_allowed",
        "state": PAPER_FILL_GATE_REASON_STATE_RECORDED,
        "reason": "PRE_TRADE_GATE_BLOCKED",
    })
churn_reason = risk_decision.get("churn_reason")
if isinstance(churn_reason, str) and churn_reason.strip():
    reasons.append(f"CHURN_BLOCKED:{churn_reason.strip()}")
    evidence_sources.append({
        "field": "risk_decision.churn_reason",
        "state": PAPER_FILL_GATE_REASON_STATE_RECORDED,
        "reason": f"CHURN_BLOCKED:{churn_reason.strip()}",
    })
fee_gate_reason = risk_decision.get("fee_gate_reason")
if isinstance(fee_gate_reason, str) and fee_gate_reason.strip():
    reasons.append(f"FEE_GATE_BLOCKED:{fee_gate_reason.strip()}")
    evidence_sources.append({
        "field": "risk_decision.fee_gate_reason",
        "state": PAPER_FILL_GATE_REASON_STATE_RECORDED,
        "reason": f"FEE_GATE_BLOCKED:{fee_gate_reason.strip()}",
    })
```

Also swap the inline literal `"paper_fill_gate_block_reason_missing_from_v2_sources"` for the new constant `PAPER_FILL_GATE_MISSING_SOURCE_REASON` (one site at replay_miner.py:524).

**Proposed tests** (`v2/backend/tests/integration/cli/test_v2_post_hoc_replay_outcome_miner.py`)

- `test_resolver_pulls_risk_decision_block_reasons` — risk_decision-only evidence ends up RECORDED.
- `test_resolver_pulls_risk_decision_churn_and_fee_strings` — `CHURN_BLOCKED:<value>` / `FEE_GATE_BLOCKED:<value>` copied verbatim with `RECORDED` lineage.
- `test_resolver_records_missing_source_when_no_evidence_anywhere` — confirm we still emit `MISSING_SOURCE` + `PAPER_FILL_GATE_MISSING_SOURCE_REASON` and never fabricate.
- `test_resolver_does_not_change_paper_fill_allowed_or_label` — protects the "no prediction made tradeable" invariant.

**Validation plan after the diff lands**

1. `py_compile` on `v2/backend/app/services/edge_proof/replay_miner.py`.
2. `pytest` on `v2/backend/tests/integration/cli/test_v2_post_hoc_replay_outcome_miner.py`.
3. CLI `v2.backend.app.cli.v2_post_hoc_replay_outcome_miner --json` to re-tag the three persisted bundle stores via the existing backfill.
4. `test_persisted_replay_bundle_stores_pass_validation_after_backfill` continues to lock the RECORDED-or-MISSING_SOURCE invariant in.

**Safety** (unchanged from task contract): `live_gate=blocked_human_only`, `live_symbols=[]`, no exchange mutation, no legacy edits, no old-Redis writes, lineage-only change.

Please grant Edit on `v2/backend/app/services/edge_proof/replay_miner.py` + `v2/backend/tests/integration/cli/test_v2_post_hoc_replay_outcome_miner.py` and Write on the per-task report path so I can land the diff and emit the report; otherwise the paired Codex review (`codex_review_autoseed_paper_fill_gate_block_reason_recording_r3`) will sit in `blocked_dependency`.
