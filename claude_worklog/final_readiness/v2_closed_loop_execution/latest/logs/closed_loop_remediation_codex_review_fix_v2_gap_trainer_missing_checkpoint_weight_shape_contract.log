The edit was denied. Before re-attempting, let me describe the planned fix and confirm.

## Proposed remediation for the Codex P1 blocker

**Problem (from `CODEX_REVIEW.md` blocker 1, P1):**
`v2/backend/app/services/rl_core/checkpoint_promotion.py` requires `tensor_shapes_per_layer.w1 == [26,16]` and `w2 == [16,5]` (input-first / `x @ W`). But operator-extracted shapes from a torch `Linear(in, out).weight` are output-first → `[16,26]` and `[5,16]`. Any honest torch sidecar would be rejected with `CHECKPOINT_METADATA_PRESENT_SHAPE_MISMATCH`, so the weight-shape contract gap is unresolved.

**Proposed changes** (all inside writable boundaries — no legacy, no exchange, live_gate stays `blocked_human_only`):

1. **`v2/backend/app/services/rl_core/checkpoint_promotion.py`** — add `tensor_shapes_per_layer_torch_native` (`w1=[16,26]`, `w2=[5,16]`), `accepted_tensor_layouts`, `matrix_layers` to `V2_POLICY_SHAPE_CONTRACT`. Update `_validate_shape_contract` to accept either layout for `w1`/`w2`, require the two matrix layers to agree on one layout, and emit `detected_tensor_layout` on each candidate so a future weight-loader knows whether to transpose. Vector layers (`b1`, `b2`, `w_exp`, `b_exp`) keep exact-match semantics.

2. **`claude_worklog/final_readiness/v2_checkpoint_promotion/latest/CHECKPOINT_PROMOTION_PROTOCOL.md`** — document both accepted layouts, with explicit note that PyTorch `Linear` stores `[out, in]`. Add `detected_tensor_layout` to the outcome description.

3. **`v2/backend/tests/integration/cli/test_v2_checkpoint_promotion_status.py`** — keep existing tests, add: (a) torch-native sidecar → `STATE_READY` + `detected_tensor_layout="torch_native_output_first"`, (b) mixed/inconsistent layout (w1 input-first, w2 torch-native) → `STATE_SHAPE_MISMATCH`, (c) `_good_metadata` still passes with `detected_tensor_layout="input_first"`.

4. **Remediation evidence packet** under `claude_worklog/.../v2_closed_loop_execution/.../codex_review_outputs/` documenting the fix, raw evidence pointers, verification commands, and that live remains blocked.

No legacy mutation, no exchange call, no torch import, no weights loaded. May I proceed with these edits?
