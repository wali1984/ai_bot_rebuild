The harness is gating the V2 file write pending your approval — same blocker that failed the previous run. To complete this remediation I need to land three artifacts, all V2-scoped, paper-only, live_gate=blocked_human_only:

1. **Edit** `v2/backend/app/cli/v2_orchestrator_arbitration_loop.py` (lines 164-212) — reorder per-cycle writes to proposals → signals:paper → decisions, build `sig_payload` before the writes, and stamp `decisions_payload["v2_orchestrator_keys_written_count"] = len(keys_written) + 1` just before the decisions write so the full-observation builder (which reads `od.get("v2_orchestrator_keys_written_count")` at `full_observation_builder.py:1611`) stops emitting `MISSING_FROM_V2_ORCHESTRATOR` for that field. Heartbeat semantics unchanged at line 233 (`len(keys_written)` stays correct because all three writes still get appended).

2. **Add** producer test `v2/backend/tests/integration/cli/test_v2_orchestrator_decisions_keys_written_stamp.py` — asserts `decisions_payload` contains `v2_orchestrator_keys_written_count` with value matching the persisted-key count (the existing consumer-side tests at `test_v2_full_observation_portfolio_state_burndown.py:296-331` already cover the builder side).

3. **Write** `claude_worklog/final_readiness/v2_full_observation_lane_payload_absent_burndown/latest/IMPLEMENTATION_REPORT.md` — per-task implementation report with evidence pointers and expected burndown delta (21 → 18 in `V2_LANE_EXISTS_PAYLOAD_ABSENT`).

Nothing touches legacy, no exchange mutation, no Redis trim, no live approval, no operator-decision-required `unified_features` extension (that first fail_blocker stays operator-gated, per its own classification).

Please approve the next Edit + Write tool calls so I can land the change and flip the codex review pair from FAIL → re-review.
