I need write permission for the baseline_model.py file. Could you grant write access to `/home/wali/Desktop/AI BOT REBUILD/v2/backend/app/services/native_trainer/baseline_model.py` (and `test_v2_native_trainer_dataset_and_baseline_model.py`)? These two files are the focus of this narrow calibration change.

Without that permission I can't proceed with the implementation. Here's what I'd change once approved:

**`baseline_model.py`:**
1. Add module-level constants `NON_TRAINABLE_LABELS_FOR_CALIBRATION`, `FALLBACK_ROUND_TRIP_COST_BPS`, `DEFAULT_CALIBRATION_BIN_COUNT`.
2. Extend `LogisticModel` with `expected_move_after_cost_bps_calibration_bins: list[dict[str, Any]]` (each bin: `lower_proba`, `upper_proba`, `mean_after_cost_return_bps`, `sample_count`), expose in `to_jsonable`.
3. Add helpers: `_calibration_eligible_rows`, `_fit_expected_move_after_cost_bps_bins`, `_predicted_expected_move_after_cost_bps_from_bins`.
4. Call the bin fit at the end of `train_logistic_model` so `LogisticModel.expected_move_after_cost_bps_calibration_bins` is populated from the same V2-owned `train_rows` used to fit weights — explicitly filtering out `insufficient_evidence` / `label_missing` / `after_cost_return_bps is None` rows.
5. Replace the hard-coded `expected_move_bps - 5.0 - 2.0` line in `build_baseline_prediction` with a bin-based lookup (fallback to `expected_move_bps - FALLBACK_ROUND_TRIP_COST_BPS` only when no bins exist).
6. Extend `BaselineMetrics` with `expected_move_after_cost_bps_calibration_mae: float | None` and `false_negative_loss_proba_separation: float | None`, populated only on the trained-logistic row (via a small post-evaluation step in `evaluate_all_baselines`).
7. Surface the two new metrics in `to_jsonable` and the markdown report.
8. Make sure all safety pins (`paper_fill_allowed=False`, `live_gate=blocked_human_only`, `live_symbols=[]`, `model_readiness=NOT_PRODUCTION_READY`, all `approves_*=False`) remain untouched.

**`test_v2_native_trainer_dataset_and_baseline_model.py`** — new tests:
- `test_trained_model_carries_after_cost_calibration_bins_only_from_v2_owned_rows` — confirms bins are populated when training on ≥64 rows and that the bins' total `sample_count` excludes any `insufficient_evidence` row mixed in.
- `test_baseline_prediction_uses_calibrated_expected_move_after_cost_bps` — confirms predicted `expected_move_after_cost_bps` falls inside the calibrated bin range, not the old `(p-0.5)*50 - 7` heuristic.
- `test_baseline_prediction_preserves_not_production_ready_after_calibration` — re-asserts every safety pin on the calibrated payload.
- `test_baseline_metrics_emit_false_negative_loss_proba_separation` — checks the new field on the trained-model row.
- `test_calibration_skipped_when_only_insufficient_evidence_rows` — fitting yields zero bins when no trainable after-cost data is present.

**Per-task report:** write `claude_worklog/final_readiness/v2_autonomous_mission_backlog/per_task/baseline_after_cost_calibration/IMPLEMENTATION_REPORT.md` summarizing the diff, the safety-pin audit, the new metric definitions, and the test list — with explicit `live_gate=blocked_human_only`, `live_symbols=[]`, `model_readiness=NOT_PRODUCTION_READY`, `v2_native_trainer_ready=false`, `model_parity_claimed=false`, `checkpoint_compatibility_claimed=false`.

Please approve the write permission for the two files above so I can apply the changes and run the targeted pytest. Nothing in this change touches legacy, Redis, exchange APIs, or any approval — it stays inside `v2/backend/app/services/native_trainer/` and its test file, plus the report path under `claude_worklog/final_readiness/...`.
