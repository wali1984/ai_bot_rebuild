I'm blocked the same way the prior r14 session was — the harness denies every `Edit`/`Write` to `v2/**` and the new `claude_worklog/.../per_task/...` path without per-file approval. The user explicitly resumed this task asking me to remediate, so I need approval to proceed.

The patch is fully designed and ready (5 files, narrow + additive). I will need write permission for these exact paths:

1. `v2/backend/app/services/edge_proof/replay_schema.py` — add `false_negative_evidence: Mapping[str, Any] | None = None` to `ReplayBundle`; add `"false_negative_evidence"` to `emit_canonical_schema()` `bundle_fields` list.
2. `v2/backend/app/services/edge_proof/replay_miner.py` — add `_false_negative_strict_gate_evidence()` helper; attach `false_negative_evidence` in `fill_outcomes()` only when label is `FALSE_NEGATIVE`; add the new field to `_PROTECTED_BUNDLE_FIELDS`.
3. `v2/backend/app/services/edge_proof/evaluator.py` — add `false_negative_block_reason_distribution` + `false_negative_missing_source_count` to `MetricSummary` and `summary_to_dict()`; lazy fallback to the miner helper for bundles pre-dating the new field.
4. `v2/backend/app/cli/v2_post_hoc_replay_outcome_miner.py` — pass `false_negative_evidence` through `_dict_to_bundle()`; surface the two new aggregates at top-level in `status_payload` / `metrics_view` / `operator_dashboard`.
5. `v2/backend/tests/integration/cli/test_v2_post_hoc_replay_outcome_miner.py` — 5 new tests (explicit-reason, MISSING_SOURCE, SHADOW_OBSERVATION_ONLY fallback, `fill_outcomes` attaches without changing label, `CORRECT_NO_TRADE` does not receive the field).
6. New file `claude_worklog/final_readiness/v2_autonomous_mission_backlog/per_task/paper_edge_false_negative_gate_reason_enrichment/IMPLEMENTATION_REPORT.md`.

Safety invariants preserved (already verified in code): strict paper-fill gate logic (`_label_from_outcome`, `_is_blocked`, `_is_trade`) untouched; `SHADOW_OBSERVATION_ONLY + after_cost > 0` still classifies as `FALSE_NEGATIVE`; no live/canary/legacy-shutdown/redis-trim approvals; no exchange or legacy mutations; `live_gate=blocked_human_only`; `live_symbols=[]`.

On approval I will: apply the 5-file patch, write the implementation report, run `pytest v2/backend/tests/integration/cli/test_v2_post_hoc_replay_outcome_miner.py -q`, then re-run the miner CLI so the three JSON outputs (`post_hoc_replay_outcome_status.json`, `edge_metrics_summary.json`, `operator_dashboard_payload.json`) surface the two new keys that the prior codex review found absent.

Please approve writes to the six paths above (or grant a blanket `v2/**` + `claude_worklog/**` edit permission for this session) and I will land the patch immediately.
