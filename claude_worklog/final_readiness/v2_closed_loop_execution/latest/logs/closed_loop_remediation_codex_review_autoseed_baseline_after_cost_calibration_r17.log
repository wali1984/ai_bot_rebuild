The harness is asking permission for an edit to `v2/backend/app/services/native_trainer/baseline_model.py` — which is on the `./v2/**` whitelist per CLAUDE.md. This is the same wall the r17 implementation worker hit. I need your approval to land the calibration changes here.

If you approve, my plan is:

1. **baseline_model.py** — add `CalibrationFit` + `ThresholdTuneResult` dataclasses; add `_fit_after_cost_calibration()` (linear regression of after-cost bps on `proba - 0.5`, refuses non-positive slope / thin samples) and `_tune_fn_separation_threshold()` (one-sided downward sweep, picks lowest threshold that reduces FN without breaching a precision floor); thread both into `train_logistic_model()`; route `build_baseline_prediction()`'s `expected_move_after_cost_bps` through `model.calibration.calibrated_after_cost(p)` while keeping the `[-25, 25]` safety clamp and every `NOT_PRODUCTION_READY` / `paper_fill_allowed=False` / `live_gate=blocked_human_only` / `live_symbols=[]` pin intact.

2. **test_v2_native_trainer_dataset_and_baseline_model.py** — add tests for: calibration slope positive on synthetic data, calibration fit_ok=False on degenerate input, FN tuner reduces FN without breaching precision floor on synthetic data, FN tuner refuses to tune below floor, insufficient-evidence rows excluded from trainable set (contract check), calibrated `expected_move_after_cost_bps` used when fit_ok, safety pins still hold post-calibration.

3. **claude_worklog/final_readiness/v2_autonomous_mission_backlog/per_task/baseline_after_cost_calibration/IMPLEMENTATION_REPORT.md** — per-task report.

4. **claude_worklog/agent_supervisor/tasks/closed_loop_remediation_codex_review_autoseed_baseline_after_cost_calibration_r17.json** — flip `status` to `completed`, set `completed_at`, `last_summary`, `terminal_classification`, release the lease.

No legacy directory writes, no exchange mutation, `live_gate=blocked_human_only` and `live_symbols=[]` throughout.

Approve the `v2/backend/app/services/native_trainer/baseline_model.py` write and I'll proceed.
