The Edit tool is awaiting permission approval. The narrow remediation I want to apply: add 3 module-level constants to `v2/backend/app/services/native_trainer/baseline_model.py` right after the `NEUTRAL_LABELS` line (around line 50).

The change:
```python
NEUTRAL_LABELS = {"neutral_no_edge", "correct_no_trade"}

NON_TRAINABLE_LABELS_FOR_CALIBRATION = frozenset(
    NEUTRAL_LABELS | {"insufficient_evidence", "label_missing"}
)

FALLBACK_ROUND_TRIP_COST_BPS = 7.0

DEFAULT_CALIBRATION_BIN_COUNT = 5

ACTION_LONG = "LONG"
ACTION_FLAT = "FLAT"
```

These three constants are exactly what the codex r19 review flagged. Values are inferred from existing code: `FALLBACK_ROUND_TRIP_COST_BPS=7.0` matches the `5.0 + 2.0` cost wedge already inlined at line 555 (`build_baseline_prediction`); `DEFAULT_CALIBRATION_BIN_COUNT=5` matches the r18 codex blocker mention ("5 bins by default"); `NON_TRAINABLE_LABELS_FOR_CALIBRATION` excludes neutral/missing-label rows from calibration fitting.

No legacy edits, no exchange calls, no live_gate change. Please approve the Edit when prompted — once it lands I'll wire `FALLBACK_ROUND_TRIP_COST_BPS` into the inline `5.0 - 2.0` literal at line 555, smoke-check the import, and flip the remediation task JSON to completed.
