Resumed `closed_loop_remediation_codex_review_autoseed_observation_gap_feature_source_burndown_r19`. Diagnosis verified end-to-end; **both attempted writes were denied by the harness permission gate** (same gate that blocked r17/r18/r19). Safety invariants preserved (`live_gate=blocked_human_only`, `live_symbols=[]`; no legacy touch; no exchange call).

## Codex FAIL diagnosis (re-verified now, evidence-linked)
- **Reader contract** — `v2/backend/app/services/rl_core/full_observation_builder.py:1625-1644` pins `v2:orchestrator:decisions` as the exact source for `portfolio_state.v2_orchestrator_keys_written_count` and emits `MISSING_FROM_V2_ORCHESTRATOR` when the field is absent. Test pin: `v2/backend/tests/integration/cli/test_v2_full_observation_portfolio_state_burndown.py:296-336`.
- **Producer defect** — `v2/backend/app/cli/v2_orchestrator_arbitration_loop.py:178-199` writes `decisions_payload` to `v2:orchestrator:decisions` *before* `keys_written` is populated (lines 190-212). The field IS published, but at line 232-246 to `v2:orchestrator:heartbeat` — wrong key. Net: 3 dims (BTCUSDT/ETHUSDT/SOLUSDT) sit in `V2_LANE_EXISTS_PAYLOAD_ABSENT`.
- **Codex `fail_blockers` is empty** with `next_action: Remediation: no_safe_scope_remediation_targets` — meaning the prior burndown lease explicitly deferred this to a paired follow-up.

## Safe V2 patch prepared (denied by gate)
After the three `_safe_write` calls finalize `keys_written`, re-publish decisions:
```
decisions_payload["v2_orchestrator_keys_written"] = list(keys_written)
decisions_payload["v2_orchestrator_keys_written_count"] = len(keys_written)
_safe_write(r, f"{V2_REDIS_PREFIX}orchestrator:decisions", json.dumps(decisions_payload), ex=600)
```
Value is the actual `len(keys_written)` from the same run — no fabrication, no zero-fill. Insert after line 212 in `v2/backend/app/cli/v2_orchestrator_arbitration_loop.py`. Existing test `test_orchestrator_emits_held_by_paper_fill_gate_with_reasons` only asserts a subset of decisions keys; adding two fields does not break it.

## Writes attempted and denied
1. `Edit v2/backend/app/cli/v2_orchestrator_arbitration_loop.py` (producer patch above) — **denied**.
2. `Write v2/frontend/public/v2_full_observation_orchestrator_keys_written_source_mapping_burndown/latest/operator_dashboard_payload.json` (evidence-only burndown payload, fully prepared in this lease) — **denied**.

## What I did not attempt (avoiding retries)
- Editing `closed_loop_remediation_codex_review_autoseed_observation_gap_feature_source_burndown_r19.json` to flip `status: running → completed` with a `remediation_verification` block. Same gate historically blocks this.

## Next action required from operator
Approve any subset and I'll resume:
- **A. Apply backend fix** — grants `Edit` on `v2_orchestrator_arbitration_loop.py`; I'll add the 3-line re-publish, add a producer-side test asserting `v2:orchestrator:decisions` carries the field, and run `pytest v2/backend/tests/integration/cli/test_v2_paper_fill_gate_block_reason_passthrough.py v2/backend/tests/integration/cli/test_v2_full_observation_portfolio_state_burndown.py`.
- **B. Evidence-only path** — grants `Write` on the public-payload path above; I'll materialize the burndown report and close the remediation JSON with `remediation_outcome=evidence_only_no_code_change_under_permission_gate` and queue paired follow-up task ids (`claude_fix_v2_orchestrator_arbitration_loop_decisions_keys_written` + codex review).
- **C. Both** — preferred; gives a real PASS on the next codex re-review instead of another evidence-only no-op cycle.

Recommendation: **C**. The diagnosis has been stable across r15-r19; another evidence-only loop will keep producing the same FAIL.
