I've been retrying the same Edit and the harness keeps returning the "haven't granted it yet" message — that means the permission prompt is sitting unanswered (or denied). I'll stop and wait for your decision rather than spam the prompt.

If you'd like me to proceed, please approve the file edits as they come up — the planned changes are exactly:

**1. `v2/backend/app/services/edge_proof/replay_miner.py`** — insert a new section between `load_filled_bundles()` and the `# Cost-model backfill + artifact validation` header. Adds:

- Constants: `EVIDENCE_STATE_EXPLICIT_BLOCK_REASON`, `EVIDENCE_STATE_MISSING_SOURCE`, `FALSE_NEGATIVE_EVIDENCE_QUALITY_SCHEMA_VERSION`, `FN_BLOCK_REASON_SOURCE_PATHS`.
- Helpers: `_explicit_block_reasons_with_provenance()`, `_paper_fill_gate_state_context()`.
- Public function: `false_negative_evidence_quality(bundle_dicts, *, primary_outcome_window_id="5m")` — pure, no I/O, no mutation, never reclassifies, never promotes FALSE_NEGATIVE → FALSE_BLOCK. Emits explicit reasons with source provenance, or `MISSING_SOURCE` when none exist on any of the three reason-list source paths. Companion `paper_fill_gate_state` is exposed for operator review but explicitly tagged with `no_inference_from_decision_state/live_gate/risk_decision`.

**2. `v2/backend/app/cli/v2_post_hoc_replay_outcome_miner.py`** — call `false_negative_evidence_quality(bundle_dicts)` after `load_filled_bundles()`, write to `WORKLOG_DIR/false_negative_evidence_quality.json` + `PUBLIC_DIR/false_negative_evidence_quality.json`, attach a one-line summary block to the status payload.

**3. `v2/backend/tests/integration/cli/test_v2_post_hoc_replay_outcome_miner.py`** — add ~5 focused tests covering: explicit reasons → EXPLICIT with correct provenance; empty → MISSING_SOURCE; non-FN bundles excluded; input not mutated; no live/canary/shutdown approval markers in output.

**4. `claude_worklog/final_readiness/v2_post_hoc_replay_outcome_miner/latest/FALSE_NEGATIVE_EVIDENCE_QUALITY_REPORT.md`** — per-task implementation report documenting: scope, what the join does, FN counts (15 total), counts per evidence_state observed against the current bundles, files touched, what was *not* changed (strict gate semantics preserved), safety pins.

**5. Regenerate** `false_negative_evidence_quality.json` by running the post-hoc miner CLI.

Please approve the file writes (or grant blanket permission for this task) and I'll land it. The CLAUDE.md write boundaries explicitly allow `./v2/**`, `./claude_worklog/**`, and `./tools/**`, so this is in-scope; the harness just needs your click.
