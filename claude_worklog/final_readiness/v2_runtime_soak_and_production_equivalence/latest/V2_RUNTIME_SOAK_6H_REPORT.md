# V2 Runtime Soak 6H Verification Report

GO/NO-GO: `V2_RUNTIME_SOAK_6H_READY`

This packet does NOT approve live trading, canary trading, leverage/margin
changes, exchange mutation, legacy shutdown, Redis trim, or the creation of
the paper-only shutdown acceptance file.

## Evaluation method

Verified from raw payloads only. No wall-clock inference. All facts cited
below come from JSON files generated by the running soak chain.

## 6h soak invariants (raw)

`soak_status.json` reports:
- `minutes_observed = 895.22` (>= 360 -> 6h target exceeded by ~9 hours)
- `soak_15m_ready = true`
- `soak_1h_ready = true`
- `soak_6h_ready = true`
- `all_v2_processes_uninterrupted = true`
- `v2_namespaces_never_empty = true`
- `observation_count = 184`
- `first_observed_utc = 2026-05-17T02:00:21Z`
- `last_observed_utc = 2026-05-17T16:55:34Z`
- `legacy_still_owns_production_observed = true`

`v2_replacement_readiness_scoreboard.json` (regenerated this run) reports:
- `v2_runtime_running = true`
- `v2_writes_v2_redis = true`
- `v2_total_redis_key_count = 36`
- `v2_soak_15m_ready = true`
- `v2_soak_1h_ready = true`
- `v2_soak_6h_ready = true`
- `shutdown_recommendation = BLOCK_LEGACY_SHUTDOWN_PARITY_INCOMPLETE`

## Passthrough gap remains closed

The earlier paper-fill-gate block-reason passthrough gap (
`paper_fill_gate_block_reason_passthrough_missing`) has Codex PASS and
remains closed.

Active Redis evidence:
- `v2:orchestrator:decisions.schema_version = v2_orchestrator_decisions_v2`
- `v2:orchestrator:decisions.held_by_paper_fill_gate[0]` = `{symbol:"SOLUSDT", paper_fill_gate_block_reasons:["NEGATIVE_EXPECTED_MOVE_AFTER_COST_BLOCK"], places_real_order:false}`
- `v2:paper:intents_held_by_paper_fill_gate[0]` = `{symbol:"SOLUSDT", decision:"HELD_BY_PAPER_FILL_GATE", paper_fill_gate_block_reasons:["NEGATIVE_EXPECTED_MOVE_AFTER_COST_BLOCK"], places_real_order:false}`
- `v2:prediction:SOLUSDT:1m` reports `paper_fill_allowed = false` with the
  same block reasons.
- `v2:prediction:BTCUSDT:1m` and `v2:prediction:ETHUSDT:1m` report
  `paper_fill_allowed = true` (no gate-block at this instant).
- No SOLUSDT fill was created from the held signal (`places_real_order=false`).

Continuous-remediation gap matrix carries the SOLUSDT row as
`paper_fill_gate_blocked_with_reason` / `NO_ACTION_REQUIRED_SAFE_BLOCK`, not
P1_FIX. `P1_FIX = 0`.

## Production-equivalence blockers remain visible

The 6h soak READY token does NOT erase any production-equivalence blocker.
The following stay visible in `legacy_log_v2_gap_matrix.json`:

- ETHUSDT / SOLUSDT `trainer_missing_checkpoint_weight_shape_contract`:
  - `V2_hold_due_strict_gate` -> `OPERATOR_DECISION_REQUIRED`
  - `checkpoint_weight_missing` -> `BLOCKS_PRODUCTION_EQUIVALENCE`
- `production_equivalence_gaps_open = 3`
- legacy still owns production runtime (raw: `legacy_still_running=true`,
  `legacy_redis_key_counts.prediction:* = 151`,
  `legacy_redis_key_counts.signals:* = 8`).

`shutdown_recommendation` stays `BLOCK_LEGACY_SHUTDOWN_PARITY_INCOMPLETE`.

## Honesty record

The V2-only rolling restart performed earlier to roll out the passthrough
fix is honestly recorded in
`continuous_remediation/ACTIVE_RUNTIME_REFRESH_AUDIT.json`. The soak
observer's 300-second cadence did not happen to land inside the 2-second
restart window, so `all_v2_processes_uninterrupted` remained `true`; the
audit JSON makes the rolling restart explicit so no observer-pass relies
on the gap being invisible.

No other unrecorded restart windows exist.

## Next gate recommendation

- `v2_runtime_soak_6h` = **READY**
- `production_equivalence` = **BLOCKED** by checkpoint-weight blob and by
  legacy still owning the production Redis namespaces.
- `legacy_shutdown` = **BLOCKED**.
- `live_trading` = `blocked_human_only`.

`next_required_fix` (from refreshed scoreboard):
> CHECKPOINT_WEIGHT_BLOB_OPERATOR_REQUIRED. 6h paper-only soak is complete
> (minutes_observed=895.22). Production-equivalence still blocked by
> missing approved checkpoint weight blob and by legacy still owning
> production Redis namespaces. Shutdown remains blocked. Live remains
> blocked_human_only.

The operator must, separately and explicitly, decide whether to:

1. Provide an approved checkpoint weight blob (per the existing
   `CHECKPOINT_WEIGHT_BLOB_OPERATOR_REQUIRED` blocker descriptor), or
2. Accept the paper-only limitation by creating
   `claude_worklog/approvals/OPERATOR_ACCEPTS_V2_PAPER_ONLY_SHUTDOWN_LIMITATIONS.md`
   manually. **This packet does NOT create that file.**

Neither path is implied by 6h soak readiness alone.

## Safety invariants (raw)

- `live_gate = blocked_human_only`
- `live_symbols = []`
- `approves_live = false`
- `approves_canary = false`
- `approves_legacy_shutdown = false`
- `approves_redis_trim = false`
- no legacy mutation
- no legacy script executed
- no exchange mutation
- no old Redis writes (V2 writes only to `v2:*`)
- no approval tokens created (paper-only shutdown acceptance file absent)

## What this packet does NOT claim

- Not live-ready.
- Not canary-ready.
- Not legacy-shutdown-ready.
- Not Redis-trim-ready.
- Does not approve the checkpoint-weight blocker.
- Does not create the paper-only shutdown acceptance file.
- Does not modify legacy in any way.
- Does not execute legacy monitor scripts.

Soak continues running. The remediation loop continues on its 5-minute
cadence.
