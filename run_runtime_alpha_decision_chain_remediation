#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "$0")"
PYTHON_BIN="${PYTHON_BIN:-./.venv/bin/python}"
if [ ! -x "$PYTHON_BIN" ]; then
  PYTHON_BIN="python"
fi
exec "$PYTHON_BIN" -m v2.backend.app.cli.run_runtime_alpha_decision_chain_remediation "$@"
