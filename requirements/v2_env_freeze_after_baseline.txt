# V2 venv freeze snapshot — to be regenerated by:
#   .venv/bin/pip freeze > requirements/v2_env_freeze_after_baseline.txt
#
# This file is intentionally a placeholder in this turn (no installs were
# performed during the Phase E env-parity audit). The next operator action
# is to run the freeze command above after the first port (market ingestor)
# completes and any per-port installs land.
#
# Until then, the V2 pyproject.toml is the authoritative dependency list:
#   v2/pyproject.toml [project.dependencies]
#     fastapi==0.115.0
#     pydantic==2.9.2
#     pydantic-settings==2.5.2
#     uvicorn==0.30.6
#     SQLAlchemy==2.0.35
#     alembic==1.13.3
#     redis==5.0.8
#     httpx==0.27.2
#     structlog==24.4.0
#     python-multipart==0.0.10
