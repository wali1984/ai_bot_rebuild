# Legacy CoinAnk Validation Report

- `py_compile`: passed before runtime restart.
- `--dry-plan`: passed; `instruments_getLastPrice` absent with `COINANK_ENABLE_LASTPRICE=0`.
- Required TFs: `5m,15m,30m,1h,4h,1d`.
