# V2 One Hour Trainer Risk Orchestrator Data Website Monitor And Explainability Report

Gate: `V2_ONE_HOUR_TRAINER_RISK_ORCHESTRATOR_DATA_WEBSITE_MONITOR_AND_EXPLAINABILITY_READY`
Generated EST: `2026-06-10T00:43:45-04:00`
Monitor samples: `61`
Final sample issues: `[]`
Live gate: `enabled_operator_approved`
Trader state: `LIVE_ARMED_BALANCE_HOLD`
Live submit blocker: `INSUFFICIENT_AVAILABLE_BALANCE_FOR_MIN_ORDER`
Native CUDA primary predictions: `655`
RL-core sidecar rows: `131`
Paper accepted fills: `5`
Open paper positions: `5`
Paper equity: `9999.97223741`
Paper PnL: `-0.02776259`

## Result

The one-hour monitor completed. Trainer, risk, orchestrator, paper, public market data, CoinAnk, liquidation levels, backend API, and website runtime publishers were active in the final sample. No real order/test-order/cancel/modify, leverage/margin mutation, old Redis write, legacy restart, Redis trim, or raw credential output occurred.

## Findings

- Predictions are generated by the native CUDA PPO/MASA trainer from current V2 feature snapshots across price action, technical indicators, orderbook liquidity, derivatives positioning, liquidation levels, and alternative-data context.
- Current model output leans mostly long: `533 long`, `55 short`, `67 hold`.
- The dominant reason predictions do not become paper/risk/orchestrator actions is confidence gating, not missing website data. Calibrated confidence ranges from about `50.58%` to `55.45%`, and `confidence_below_threshold` appears on `630` blocked prediction rows.
- Paper accounting is moving: current session equity is no longer pinned at exactly `10000.0`; it is `9999.97223741` with `5` economic open paper positions.
- Early-hour stale state issues were fixed or cleared by the end: invalid symbols are now `0`, live gate in pipeline status is current, runtime truth trainer status is current, and final price rows have `0` stale over two minutes.

## Website And Controls

- Backend API is active on `http://127.0.0.1:5173`.
- Local frontend is active on `http://127.0.0.1:5177`.
- Local `/api` proxy is enabled and `/api/v2/pipeline/status` returns HTTP `200` through the frontend origin.
- Local route crawl after proxy repair: `32/32`.
- Production route crawl: `32/34`; remaining markers are `/admin/monitor-center?role=admin` and `/admin/script-registry?role=admin` redirect/proof-dump classifications. Production still shows the current runtime banner with `enabled_operator_approved` and `INSUFFICIENT_AVAILABLE_BALANCE_FOR_MIN_ORDER`.

## Natural Language Explainability

Created current explanation payload:

`/operator_runtime/v2_prediction_signal_explanations/latest/prediction_signal_explanations.json`

Website pages now surface plain-English descriptions for prediction/signal rows and safe pipeline tasks. The explanation tells the operator what data families were present, what PPO/MASA thought, why the row is useful or blocked, and what can improve the signal without touching live execution.

## Validation

- py_compile: `PASS`
- focused backend tests: `PASS: 30 passed`
- frontend typecheck: `PASS`
- frontend build: `PASS`
- local route crawl: `PASS: 32/32`
- production route crawl: `PASS_WITH_KNOWN_MARKERS: 32/34`
- raw secret scan: `PASS`
- old Redis scan: `PASS for touched code`
- exchange mutation scan: `PASS for touched code`

## Residual Notes

- Live remains correctly balance-held. The next live trigger is usable available margin.
- The stale-price monitor issue appeared during the hour because the REST ingestor wrote after full-batch completion. It was patched to write each completed symbol immediately and its cadence was tightened.
- `ai-bot-v2-no-status-change-sla-watchdog.service` now treats its intentional blocked-status exit code as a successful oneshot result, so `systemctl --user --failed` is clear. Its JSON can still report stale event-watcher evidence separately; that is not a trainer/risk/orchestrator/paper/live data-path failure in the final monitor sample.
