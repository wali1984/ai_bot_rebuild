import type { PageMeta } from '../../types/page';

const meta: PageMeta = {
  id: 'executive-status',
  title: 'Executive Status',
  surface: 'admin',
  description:
    'Plain-English executive truth: are we migrated, can legacy shut down, can we go live, what is blocking, what is the next action. Clarity layer only; live, shutdown, and symbol adoption stay blocked.',
  navCategory: 'audit',
  dangerousControlIds: [],
};

export default meta;
