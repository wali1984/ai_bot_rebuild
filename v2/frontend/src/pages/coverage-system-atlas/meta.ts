import type { PageMeta } from '../../types/page';
const meta: PageMeta = {
  id: 'coverage-system-atlas',
  title: 'Coverage / System Atlas',
  surface: 'admin',
  description: 'File inventory and classification for legacy + V2 surfaces.',
  navCategory: 'audit',
  dangerousControlIds: [],
};
export default meta;
