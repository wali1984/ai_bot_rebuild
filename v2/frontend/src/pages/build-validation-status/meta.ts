import type { PageMeta } from '../../types/page';
const meta: PageMeta = {
  id: 'build-validation-status',
  title: 'Build/Validation Status',
  surface: 'admin',
  description: 'Lists claude_worklog/v2_build/* artifacts and READY/BLOCKED states.',
  navCategory: 'audit',
  dangerousControlIds: [],
};
export default meta;
