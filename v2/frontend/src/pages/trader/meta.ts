import type { PageMeta } from '../../types/page';

const meta: PageMeta = {
  id: 'trader',
  title: 'Trade',
  surface: 'app',
  description: 'Trading workspace with order book, depth chart, recent trades, positions, and account-scoped market evidence.',
  navCategory: 'execution',
  dangerousControlIds: [],
};

export default meta;
