import type { PageMeta } from '../../types/page';

const meta: PageMeta = {
  id: 'history',
  title: 'History',
  surface: 'app',
  description: 'Execution order and signal history with source freshness and account-scoped availability.',
  navCategory: 'portfolio',
  dangerousControlIds: [],
};

export default meta;
