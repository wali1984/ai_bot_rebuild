import type { PageMeta } from '../../types/page';

const meta: PageMeta = {
  id: 'alerts',
  title: 'Alerts',
  surface: 'app',
  description: 'Price, OI, liquidation, funding, signal, and account-risk alert readiness.',
  navCategory: 'alerts',
  dangerousControlIds: [],
};

export default meta;
