import type { PageMeta } from '../../types/page';
const meta: PageMeta = {
  id: 'login',
  title: 'Sign in',
  surface: 'public',
  description: 'Session creation. CSRF-protected. No internal IDs leaked.',
  navCategory: 'public',
  dangerousControlIds: [],
};
export default meta;
