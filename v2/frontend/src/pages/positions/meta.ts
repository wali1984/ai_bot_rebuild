import type { PageMeta } from '../../types/page';
const meta: PageMeta = {
  id: 'positions',
  title: 'Positions',
  surface: 'admin',
  description: 'Open positions (paper-only by default).',
  navCategory: 'execution',
  dangerousControlIds: [],
};
export default meta;
