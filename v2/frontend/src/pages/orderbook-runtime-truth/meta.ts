import type { PageMeta } from '../../types/page';

const meta: PageMeta = {
  id: 'orderbook-runtime-truth',
  title: 'Orderbook Runtime Truth',
  surface: 'admin',
  description: 'Direct orderbook feed, replay store, and feature-consumption runtime status.',
  navCategory: 'data',
  dangerousControlIds: [],
};

export default meta;
