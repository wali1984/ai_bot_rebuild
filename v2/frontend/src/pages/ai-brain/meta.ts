import type { PageMeta } from '../../types/page';

const meta: PageMeta = {
  id: 'ai-brain',
  title: 'Model State',
  surface: 'system',
  description: 'System-only model health, forecast quality, uncertainty, and latest signal context with no live approval.',
  navCategory: 'signals',
  dangerousControlIds: [],
};

export default meta;
