import type { PageMeta } from '../../types/page';

const meta: PageMeta = {
  id: 'risk',
  title: 'Risk',
  surface: 'app',
  description: 'Read-only trader view of liquidation buffer, hedge state, squeeze risk, kill switch, and live gate truth.',
  navCategory: 'portfolio',
  hideFromNav: true,
  dangerousControlIds: [],
};

export default meta;
