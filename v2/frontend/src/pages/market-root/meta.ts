import type { PageMeta } from '../../types/page';

const meta: PageMeta = {
  id: 'market-root-alias',
  title: 'Market Alias',
  surface: 'public',
  description: 'Hidden compatibility alias that redirects /market to the canonical /markets route.',
  navCategory: 'public',
  hideFromNav: true,
  dangerousControlIds: [],
};

export default meta;
