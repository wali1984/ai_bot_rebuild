import type { PageMeta } from '../../types/page';
const meta: PageMeta = {
  id: 'user-status',
  title: 'Simple Status',
  surface: 'public',
  description:
    "Plain-English status. What the bot is doing today, why live is blocked, and what needs to happen next. No internal IDs and no live controls.",
  navCategory: 'status',
  dangerousControlIds: [],
};
export default meta;
