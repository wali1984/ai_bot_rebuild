import type { PageMeta } from '../../types/page';

const meta: PageMeta = {
  id: 'markets',
  title: 'Markets',
  surface: 'public',
  description: 'Alias route for the implemented public market page. Missing and stale data remain visible.',
  navCategory: 'public',
  dangerousControlIds: [],
};

export default meta;
