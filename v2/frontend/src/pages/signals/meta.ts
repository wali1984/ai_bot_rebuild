import type { PageMeta } from '../../types/page';
const meta: PageMeta = {
  id: 'signals',
  title: 'Signals',
  surface: 'app',
  description: 'Recent signals with lineage chain rendering.',
  navCategory: 'market',
  dangerousControlIds: [],
};
export default meta;
