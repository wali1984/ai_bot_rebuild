import type { PageMeta } from '../../types/page';

const meta: PageMeta = {
  id: 'markets-ingestors',
  title: 'Market Ingestors',
  surface: 'public',
  description:
    'Realtime chart view of every V2 market data ingestor: status, freshness, and ingested data streamed over WebSocket. Missing and stale data remain visible.',
  navCategory: 'public',
  dangerousControlIds: [],
};

export default meta;
