import type { PageMeta } from '../../types/page';
const meta: PageMeta = {
  id: 'executions',
  title: 'Executions',
  surface: 'admin',
  description: 'Execution intents and fills (paper-only by default).',
  navCategory: 'execution',
  dangerousControlIds: [],
};
export default meta;
