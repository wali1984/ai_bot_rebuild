import type { PageMeta } from '../../types/page';
const meta: PageMeta = {
  id: 'executions',
  title: 'Executions',
  surface: 'app',
  description: 'Execution intents and fills with account-scoped runtime evidence.',
  navCategory: 'execution',
  dangerousControlIds: [],
};
export default meta;
