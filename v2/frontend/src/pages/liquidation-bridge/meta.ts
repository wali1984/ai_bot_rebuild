import type { PageMeta } from '../../types/page';
const meta: PageMeta = {
  id: 'liquidation-bridge',
  title: 'Liquidation Runtime / Levels',
  surface: 'admin',
  description: 'Live status of the V2 liquidation WSS and levels engine. Shows forceOrder event stream and computed liquidation level proximity without a current ingestor bridge.',
  navCategory: 'data',
  dangerousControlIds: [],
};
export default meta;
