import type { PageMeta } from '../../types/page';

const meta: PageMeta = {
  id: 'microstructure-trust',
  title: 'Microstructure Trust',
  surface: 'admin',
  description: 'Adversarial orderbook trust, sweep risk, feed quality, and candidate block evidence.',
  navCategory: 'risk',
  dangerousControlIds: [],
};

export default meta;
