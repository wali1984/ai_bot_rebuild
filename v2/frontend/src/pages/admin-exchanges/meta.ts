import type { PageMeta } from '../../types/page';
const meta: PageMeta = {
  id: 'admin-exchanges',
  title: 'Exchanges',
  surface: 'admin',
  description: 'Exchange connectivity, REST/WS status, rate limits, masked credential state, and allowed balances.',
  navCategory: 'exchanges',
  navLabel: 'Exchanges',
  navOrder: 70,
  dangerousControlIds: [],
};
export default meta;
