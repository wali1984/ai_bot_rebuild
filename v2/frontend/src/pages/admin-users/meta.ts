import type { PageMeta } from '../../types/page';
const meta: PageMeta = {
  id: 'admin-users',
  title: 'Users',
  surface: 'admin',
  description: 'User list, roles, sessions, and account state management.',
  navCategory: 'users',
  navLabel: 'Users',
  navOrder: 90,
  dangerousControlIds: [],
};
export default meta;
