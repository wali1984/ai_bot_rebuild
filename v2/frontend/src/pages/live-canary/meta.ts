import type { PageMeta } from '../../types/page';

const meta: PageMeta = {
  id: 'live-canary',
  title: 'Live Canary',
  surface: 'app',
  description: 'Read-only trader view of canary readiness, A+ inventory, dry-run order builder, hedge plan, and no-mutation flags.',
  navCategory: 'trade',
  hideFromNav: true,
  dangerousControlIds: [],
};

export default meta;
